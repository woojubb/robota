{
  "name": "robota-capability-agent-eval",
  "version": "0.1.0",
  "private": true,
  "description": "Robota capability demo — evals-as-code: define metrics over an agent's runs and gate CI on a metric breach",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@robota-sdk/agent-framework": "^3.0.0-beta.67",
    "@robota-sdk/agent-provider": "^3.0.0-beta.67"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.23.1",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
