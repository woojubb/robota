# 스펙 인덱스 (최신)

이 인덱스는 `.design`의 정식 스펙 문서 진입점입니다.

## 스펙 목록
- `event-system.md`: EventService / EventContext(ownerPath-only) / payload vs context / 이벤트 상수 규칙 / 실패 분류 / event record SSOT
- `workflow-graph.md`: 워크플로우 그래프 규칙 + fork/join(원자적 처리) + Node/Edge Creation Contract
- `web-host.md`: `apps/web`(Playground 호스트) 스펙
- `playground.md`: `packages/playground` 스펙(시각화/Tools DnD 포함)
- `remote.md`: `@robota-sdk/remote` 스펙
- `type-ownership.md`: 타입 소유권(SSOT) + T/I prefix 규칙
- `type-ownership-inventory.md`: 타입 소유권 감사 인벤토리(범위/검사 기준/결과)
- `terminology.md`: 프로젝트 용어 SSOT(실패 분류, linkage 용어 포함)
- `skills-directory.md`: rules/skills 분리 운영 기준


