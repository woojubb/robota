/**
 * @deprecated
 * This example is intentionally DISABLED and MUST NOT be executed again.
 *
 * Intent:
 * - Keep the historical filename for reference only.
 * - Prevent accidental execution from scripts/CI/manual runs after the refactor.
 *
 * Use instead:
 * - `13-guarded-edge-verification.ts` (refactor validation via scenario playback)
 * - `15-continued-conversation-edge-verification.ts` (continued-conversation validation)
 */

throw new Error(
    '[DEPRECATED-EXAMPLE] apps/examples/14-playground-edge-verification-deprecated.ts is disabled. ' +
    'Do not run it. Use 13-guarded-edge-verification.ts instead.'
);

