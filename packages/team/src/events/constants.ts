/**
 * Team container event constants (TO BE REMOVED)
 * 
 * [PATH-ONLY] These events will be completely removed as team is third-party
 * and should not emit events. This file exists temporarily for build compatibility.
 */

// Team events removed (team is third-party and must not emit events)
export const TEAM_EVENTS = {} as const;

// Legacy constants - TO BE REMOVED after migration
export const TOOL_EVENTS = {} as const;
