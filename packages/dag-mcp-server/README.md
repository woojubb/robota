# @robota-sdk/dag-mcp-server

MCP server for Robota DAG orchestration APIs.

```bash
ROBOTA_DAG_SERVER_URL=http://localhost:3012 robota-dag-mcp
```

The server exposes JSON-first DAG definition, node catalog, and run lifecycle tools over MCP stdio.
