// Interaction-channel contracts — SSOT relocated to @robota-sdk/agent-interface-transport (DATA-001).
export type {
  InteractionEvent,
  IPermissionRequest,
  IActionRequest,
  IActionResponse,
  IPickItem,
  ICommandInfo,
  ICommandInteractionHint,
} from '@robota-sdk/agent-interface-transport';
