/**
 * CLI-specific types for terminal I/O abstraction.
 * ITerminalOutput and ISpinner are owned by agent-core (domain port).
 */
export type { ISpinner, ITerminalOutput } from '@robota-sdk/agent-core';
