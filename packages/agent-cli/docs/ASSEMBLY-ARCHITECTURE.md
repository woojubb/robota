# Agent CLI Assembly Architecture

This file is retained as a stable pointer for older references.

The current master architecture map is
[ARCHITECTURE-MAP.md](ARCHITECTURE-MAP.md). That document is source-verified
against current CLI composition and includes package diagrams, execution flows,
command layering, provider/model state flow, and layer audit findings.

Do not add new architecture details here. Update `ARCHITECTURE-MAP.md` and the
owning package `SPEC.md` instead.
