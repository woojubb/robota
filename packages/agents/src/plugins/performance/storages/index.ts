export { MemoryPerformanceStorage } from './memory-storage';

// Note: Additional storage implementations (file, prometheus, remote, silent) 
// can be added here following the same pattern as other plugins 