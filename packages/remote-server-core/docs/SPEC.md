# Remote Server Core Specification

## Scope
- Owns reusable Remote API server routes and server-side composition behavior for Robota.

## Boundaries
- Does not own remote client contracts or app-specific deployment behavior.
- Keeps reusable remote server wiring separate from application entrypoints.
