# @robota-sdk/agent-command-help

Composable `/help` command module for Robota hosts.

The package owns command metadata and execution. It consumes SDK command-list formatting APIs from `@robota-sdk/agent-sdk`.
