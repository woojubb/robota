# @robota-sdk/agent-command-background

Composable `/background` command module for listing, reading, cancelling, and closing Robota background tasks.

The package owns command metadata and execution. It consumes command-facing background task APIs from `@robota-sdk/agent-sdk`.
