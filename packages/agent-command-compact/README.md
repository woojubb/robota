# @robota-sdk/agent-command-compact

Composable `/compact` command module for Robota sessions.

```ts
import { createCompactCommandModule } from '@robota-sdk/agent-command-compact';
```

Hosts compose the module into `InteractiveSession` through the SDK command-module API.
