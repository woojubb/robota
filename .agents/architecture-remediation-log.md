# Architecture Remediation Log

Living list of **code-side** architecture findings surfaced by the `architecture-refresh` loop
(the `architecture-auditor` / `architecture-conformance-auditor` agents) that require a **gated code
change** and cannot be resolved by a documentation fix. Doc-side drift is fixed in-loop by
`architecture-fixer` and does not appear here; this log tracks the code changes that keep the
implementation in sync with the intended architecture.

Each item is a candidate backlog entry: verify it still holds, then route it through the repo's
spec/gate process (or assign it to `architecture-implementer` under that process). Mark items resolved
(and delete) once landed.

## Open — from pass 1 (agent-\* product layers, 2026-07-07)

_None — all pass-1 code-side items are Resolved (below)._

### Design alternatives (not defects — decision items)

- **agent-interface-transport pure accessors — DECIDED (A-keep), mechanized by INFRA-035.** The package
  ships four pure derivation accessors (`readAssistantReplies`, `readLastAssistantText`, `readToolCalls`,
  `readErrors`). A neutral `architecture-auditor` pass recommended **keep** the "contracts + pure
  derivations" model (the accessors are verifiably pure, single-owned, used by only two consumers;
  relocating them to `agent-transport` would push contract-pure `-ws`/`-http` siblings toward a runtime
  dependency on the heavier transport or duplicated filters — an SSOT/coupling regression). The invariant
  that matters (zero runtime dependency edges / no runtime constructs) is now enforced mechanically by
  `scripts/harness/scan-interface-runtime.mjs` (`harness:scan` → `interface-runtime`, INFRA-035), not by
  standing prose. No relocation. Closed.

## Open — pass 2 (interface & boundary audit, 2026-07-08)

Code-side + guard findings from the whole-repo interface/boundary audit (7 auditors over disjoint areas

- a global design pass). Doc-side drift from the same audit is applied in-loop by `architecture-fixer`
  and is not listed here. Each item is a candidate backlog entry: re-verify, then route through the
  spec/gate process. Several are **decision items** (a real fork the proposal-reviewer/user must pick),
  flagged as such.

| ID     | Severity | Area                | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Proposed fix (gate on pickup)                                                                                                                                                                                                                                                          |
| ------ | -------- | ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ARL-08 | **high** | session/framework   | **Real bug.** `agent-framework/src/interactive/session-persistence.ts`: `toSessionRecord` = `{ ...session }` writes `goal` to disk, but `fromSessionRecord` enumerates fields explicitly and omits `goal` — an in-flight autonomous goal (GOAL-001) is silently lost on resume. Verified: `goal` appears nowhere in `fromSessionRecord`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Map `goal` in `fromSessionRecord`; add a mapper-completeness test asserting every `IInteractiveSessionRecord` field round-trips; ideally derive the mapping so drift is structurally impossible (see ARL-09).                                                                          |
| ARL-09 | medium   | foundation/session  | Two parallel session-record contracts: `agent-session`'s storage-neutral `ISessionRecord`/`ISessionStore` (opaque `messages: unknown[]`) and `agent-interface-transport`'s typed `IInteractiveSessionRecord`/`IInteractiveSessionStore` (DATA-001, adds `goal`, `skillActivationEvents`, …). The consumer-facing store returns the typed one; a hand-maintained mapper bridges them and has already drifted (ARL-08). **Decision item.**                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Either collapse to one record contract, or formally document the storage-neutral-vs-typed two-layer split as intentional in `cross-cutting-contracts.md` + add a mapper-completeness test. Root cause of ARL-08. Guard: ARL-16(c).                                                     |
| ARL-10 | **high** | provider/dag-nodes  | Provider granularity inversion. `agent-provider` is a monolith statically bundling all vendor SDKs; each per-vendor `dag-node-llm-text-*` depends on the whole monolith AND imports the concrete provider class (`new OpenAIProvider()`) instead of `agent-core`'s `IAIProvider`/`AbstractAIProvider` with injection. → closure bloat (openai node drags Anthropic/Google SDKs) + concrete coupling; the 5 llm-text nodes copy-paste glue. **Decision item.**                                                                                                                                                                                                                                                                                                                                                                                                                                        | Split `agent-provider` per-vendor OR make vendor SDKs optional/peer deps; invert `dag-node-llm-text-*`/media nodes onto `IAIProvider` + composition-root injection; extract a shared `AbstractLlmTextNodeDefinition`.                                                                  |
| ARL-11 | medium   | dag-nodes           | DAG leaf→assembly reach + node→node. `dag-node-skill` imports concrete `executeSkill`/`SkillCommandSource` from `agent-framework` (top assembly layer — a leaf coupled to the least-stable package); `dag-node-llm-text-router` depends on 5 sibling node packages (only node→node edge; breaks the leaf/uniform-family invariant).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Introduce a stable contract (port owned by `agent-core`/`agent-tools` or an `agent-interface-*` package) for the skill/tool execution capability; move the router's multi-provider fallback above the leaf layer (or sanction+allowlist the aggregator). Enforce via ARL-16(b).        |
| ARL-12 | medium   | dag-framework       | `dag-framework`'s default node registry statically imports 19 node packages (`dag-cli` adds 6 more), coupling the assembly layer to every node and bypassing the `IDagNodeManifest` port.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Extract a `dag-nodes-default` registry package composing the manifests; make `dag-framework` registry-injected (`createDagFramework({ nodes })`), depending on no node package directly. Same injection seam ARL-10 needs.                                                             |
| ARL-13 | medium   | dag-api/orch-client | DAG HTTP wire contract (response envelope + RFC-7807 problem-details) defined twice — server `dag-api` and client `dag-orchestration-client` — no shared SSOT, no dep edge, and already diverging (`IProblemDetails` requires `code`/`retryable`; the client marks both optional).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Extract the wire envelope + problem-details as one contract (owned by `dag-core` or a dedicated contract module) imported by both ends; or document a deliberate superset/subset split and pin the shared SSOT. Guard: ARL-16(c).                                                      |
| ARL-14 | medium   | interface/transport | `agent-interface-tui` is a single-consumer/single-implementor contract package (only `agent-transport-tui` consumes/implements its 6 types) — an interface package earns existence by decoupling ≥2 implementors; and `agent-transport-tui/src/command-interaction.ts` is a **pure pass-through re-export** of those 6 types (no-pass-through rule violation, verified).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Fold the 6 types into `agent-transport-tui` (`src/contracts`) unless a second TUI implementor is committed; remove the pass-through re-export + repoint consumers to import from `@robota-sdk/agent-interface-tui` directly. Guard: ARL-16(d).                                         |
| ARL-15 | low      | repo hygiene        | 24 empty **husk** package dirs (no `package.json`) contradict the inventory SSOT: never-executed split scaffolding (`agent-plugin-*` ×9, `agent-provider-*` ×6) + absorbed/renamed husks (`agent-runtime`, `agent-sdk`, `agent-sessions`, `agent-team`, `agent-transport-headless`, `agent-command-agent`, `agent-event-service`, `dag-designer`, `dag-orchestrator`). **Product/cleanup decision** (not a grep cleanup).                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Delete the husk dirs (+ the dead `.changeset/pre.json` `agent-transport-headless` entry); write down the family-decomposition policy (split only on independent heavy deps/release) and reconcile the plugin/provider families to it.                                                  |
| ARL-16 | medium   | harness (guard C5)  | Boundary guard coverage gaps — the "assert the relation, not a token" mechanization for boundaries this audit found unguarded. (a) doc dependency-**edge** guard (map mermaid/prose dep claims ↔ actual `package.json` edges — `check-architecture-conformance` only checks name-token existence); (b) `dag-nodes-leaf` rule in `check-dependency-direction.mjs`; (c) structural-duplicate-contract check (ARL-09/ARL-13); (d) extend `check-interface-imports.mjs` to every `agent-interface-*` export set (only `-transport` today — ARL-14 was invisible); (e) base `agent-command` + command→sibling-command forbidden pattern in `check-command-layering.mjs`; (f) browser-safety guard for `agent-playground`/`agent-web-ui` `./client`; (g) fix `check-sdk-react-free.mjs` docstring-vs-scanned-package + the phantom type-guard comment in `agent-interface-tui/src/command-interaction.ts`. | Each realizes [harness-governance](.agents/skills/harness-governance/SKILL.md) "Assert the relation, not a proxy". File as one or more guard-hardening backlog specs (see the INFRA-DOC-GUARD-001 pattern); land under the same frozen-baseline discipline where a burndown is needed. |

## Resolved

| ID     | Severity | Area                 | Finding                                                                                                                                                                                                                                                                                                                                                                                   | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------ | -------- | -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ARL-01 | medium   | foundation           | `ToolRegistry` and `FunctionTool` were **duplicated** across `agent-core/src/tool-registry/` and `agent-tools/src/`, with the `FunctionTool` validation paths diverging (`additionalProperties`). SSOT breach.                                                                                                                                                                            | **DATA-005** (`.agents/spec-docs/done/DATA-005-toolregistry-functiontool-ssot.md`). `agent-core` is now the single owner: the `additionalProperties`-aware validator was relocated into `agent-core/src/tool-registry/parameter-validator.ts` (characterization-tested, RED→green), both classes are barrel-exported from `@robota-sdk/agent-core`, the `agent-tools` copies + duplicate validator were deleted, its `createFunctionTool`/`createZodFunctionTool` factories construct core's `FunctionTool`, and consumers import the classes from core. No pass-through re-export.                                                                                                                                                                                                                                                                                                                                                                                                      |
| ARL-02 | medium   | session/execution    | `GitWorktreeIsolationAdapter` (concrete `git` CLI + fs I/O) lived in `agent-executor`, whose SPEC boundary says it creates no worktrees (self-flagged as ARCH-FIX-024). `agent-subagent-runner` also hard-defaulted the concrete adapter.                                                                                                                                                 | **INFRA-031** (`.agents/spec-docs/done/INFRA-031-worktree-adapter-to-composition-root.md`), executing the deferred ARCH-FIX-024. The concrete `GitWorktreeIsolationAdapter` + `createGitWorktreeIsolationAdapter` moved to the composition root `agent-cli/src/subagents/git-worktree-isolation-adapter.ts` (its test moved with it); `agent-executor` keeps only the `ISubagentWorktreeAdapter` port + the pure `WorktreeSubagentRunner` decorator (its "creates no Git worktrees" boundary is now literally true — `execFileSync`/`mkdirSync` gone from its src). `agent-subagent-runner` dropped the concrete-git default: `worktreeAdapter` is now a **required** injected option (breaking; changeset recorded). `agent-cli` injects the adapter at `createChildProcessSubagentRunnerFactory` via a direct import (composition-root allowlist entry in the `cli-agent-executor-import` conformance scan; no pass-through re-export).                                                |
| ARL-03 | medium   | provider/command/CLI | Preset `enabledCommandModules`/`disabledCommandModules` filter on the `agent-command-*` module-name form; a plausible short name (`editor`) is **silently ignored** (no validation, no error) — on both the startup `--preset` and in-session `/preset` paths, with the filter duplicated across `agent-command`'s `applyModuleSelection` and `agent-framework`'s `selectCommandModules`. | **INFRA-032** (`.agents/spec-docs/done/INFRA-032-preset-module-selection-validation.md`). Detection now lives once as the pure `findUnknownModuleNames(availableNames, enabled?, disabled?)` in the lower shared layer (`agent-framework`), beside `selectCommandModules`; `agent-command`'s duplicate `applyModuleSelection` collapses to delegate to `selectCommandModules`. `createDefaultCommandModules` returns `{ modules, unknownModuleNames }`; `buildCommandSetup` (agent-cli) forwards the unknowns and `cli.ts` writes a non-fatal terminal notice per unmatched name. The in-session `/preset` path threads the same unknowns through the `applyCommandModuleSelection` host-context seam and `IPresetApplicationResult.unknownCommandModules`, surfaced in the `/preset` command result. No hardcoded name list — the available set derives from the built module `name`s. Non-fatal on both paths; changeset covers `agent-framework`/`agent-command`/`agent-cli` (minor). |
| ARL-04 | low      | transport            | HTTP `/submit` completion resolved only inside the `thinking(false)` handler (temporal coupling); the `complete`/`interrupted`/`error` handlers never resolved — it worked only via a trailing `thinking(false)`.                                                                                                                                                                         | **INFRA-033**. The `complete`/`interrupted`/`error` handlers in `agent-transport-http/src/routes.ts` are now `async` and `await stream.writeSSE(...)` before `resolve()` (flush the terminal event ahead of resolve→cleanup→stream-close, since `writeSSE` is fire-and-forget); `error` resolves (streams the SSE `error` event), not rejects; the `thinking`-coupled resolve is removed. Tests (RED-without-fix confirmed) assert the client receives the terminal event with no trailing `thinking(false)`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ARL-05 | low      | foundation           | `agent-tool-mcp` declared an unused `@robota-sdk/agent-tools` devDependency (imported nowhere), contradicting its agent-core-only SPEC boundary.                                                                                                                                                                                                                                          | **INFRA-034**. Removed the unused devDependency from `agent-tool-mcp/package.json`; the manifest now matches reality (agent-core only). Authoritative check: zero imports (`rg`) + build + typecheck green (no repo scan detects unused declared deps). No changeset (private package, no code/API change).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ARL-06 | low      | transport            | `agent-web-ui` module-locally re-exports `TServerMessage`/`TClientMessage` from `agent-transport-ws` (borderline; not a root pass-through).                                                                                                                                                                                                                                               | **INFRA-033** (accept-and-watch). Reviewed: the re-export is module-local in `ws-session-client.ts`, NOT surfaced from `agent-web-ui/src/index.ts`, so it is not a public pass-through — acceptable as-is. No code change; the invariant (do not surface these from `index.ts`) stands as a watch item.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ARL-07 | low      | transport            | `agent-transport-http/src/routes.ts` header comment claimed each endpoint maps 1:1 to an IInteractiveSession API method, but the HTTP surface is a strict subset.                                                                                                                                                                                                                         | **INFRA-033**. Reworded the `routes.ts` header comment to "Exposes the core session methods (a subset; background-task, job-group, and execution-workspace methods are WS-only)." (No SPEC edit — the package SPEC carries no 1:1 claim.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
