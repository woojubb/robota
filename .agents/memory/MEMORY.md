# In-Repo Agent Memory — Index

Durable, cross-session agent knowledge for this repo. Governed by
[`../rules/memory-mirroring.md`](../rules/memory-mirroring.md): anything written to an agent's session/host
memory MUST also be mirrored here so every clone shares the same harness knowledge. One line per fact below;
each fact is a file in this directory. One owner per fact — if an existing rule/spec/lesson owns it, link there.

- Product north-star (owner: [`../../VISION.md`](../../VISION.md)) — **Robota builds Robota** means: make `robota` a **general** development agent capable enough to build even Robota. Building Robota is the **validation benchmark** (hardest dogfood), **NOT** a directive to build a Robota-specific/dedicated tool — Robota-specific content in `packages/` = moving AWAY from the goal (owner clarification 2026-07-18). So library neutrality (TRANS-001, project-structure.md) IS the goal restated; capabilities are neutral mechanisms usable on any project, Robota-specific assembly stays in agent-cli/apps. Capability roadmap = `.agents/tasks/SELFHOST-*`. Surfaced in AGENTS.md + README + VISION.md "What this does NOT mean".
- [SELFHOST roadmap progress](selfhost-roadmap-progress.md) — all 14 SELFHOST specs DESIGN-GATED (ENDORSE) + promoted to todo/ (PRs #1190, #1191); **SELFHOST-001 DONE** (5 orchestration primitives #1192/#1194/#1195), **SELFHOST-002 DONE** (plan-mode #1197/#1198), **SELFHOST-003 v1 DONE** (codebase retrieval: repo-map contract+tool+adapter #1200, index+persistence #1202, incremental re-index #1203; P4 vector-backend DEFERRED to a backlog) — all spec-docs/done/, tasks archived, all gates incl. user-execution scenarios. NEXT: remaining specs from todo/ in priority order (consider SELFHOST-004 trace/cost view next) (2026-07-18)
- Worktree-parallel subagent orchestration (owner: [`../rules/git-branch.md`](../rules/git-branch.md) § Git Worktree) — OPERATIONAL (waves 1–3, #1288–#1296, zero merge conflicts). Pattern: partition file ownership BEFORE spawning (owned + forbidden lists) → isolation:worktree → each agent branches/verifies/PRs, orchestrator merges sequentially. Incident fixes: GIT_DIR leak (#1293 envWithoutGitVars), hook worktree-blindness (#1294). See [`worktree-parallel-orchestration.md`](worktree-parallel-orchestration.md).
- Harness diet audit (2026-07-23) — 8-agent audit; dominant defect NON-NEUTRALITY. **COMPLETE 2026-07-24**: all 7 sub-items done+archived (waves 1–4, #1277–#1302; scans 63→59, 13 skills removed + 16 slimmed, hooks/rules/routing dieted). Re-auditing is PERMITTED (owner, 2026-08-01) — the ban was written when the sweep had just finished and nothing had been added since; a great deal has been added since, and a standing ban on looking again is itself the kind of rule that outlives its reason. See [`harness-diet-audit.md`](harness-diet-audit.md).
- [Rule contradictions are improved continuously](rule-contradictions-continuous.md) — pointer only. Owner: [`../rules/learning-loop.md`](../rules/learning-loop.md) § Contradiction Between Rules (owner directive 2026-08-03); mechanism gap = [HARNESS-072](../tasks/HARNESS-072-nothing-detects-a-contradiction-between-two-rules.md) / issue #1617
- [Review rounds run until a round returns zero](review-rounds-until-zero.md) — pointer only. Owner: [`../skills/pr-finding-resolution-loop/SKILL.md`](../skills/pr-finding-resolution-loop/SKILL.md) Round B step 4 (owner directive 2026-08-03); general form in [`../rules/enforcement-architecture.md`](../rules/enforcement-architecture.md)
- [Comment-asserted invariants](comment-asserted-invariants.md) — the dominant defect class on PR #1600: 4 comments asserted properties the code lacked, one recurring 5× across 8 review rounds, and each claim stopped the next reader from checking. Owner 2026-08-02: reflection counts only when a universal + neutral MECHANICAL prevention is implemented AND verified. Mechanism landed and red-proved: `scan-authority-bypass` (config-driven, registered, 11 own tests). common-mistakes #83
- [A commit message that describes intent instead of the diff](claimed-without-reading-back.md) — twice on 2026-08-02 (#1605, #1606) a commit message asserted an edit the tree did not contain: a formatter undid a revert, and a `branch-guard`-rejected COMPOUND command ran none of itself. Review caught both. Write the sentence from `git diff --staged`, verify POST-hook, and never cite two tool runs that measured different trees
- [Finding depth, and the two properties a guard has when it is not blocking](finding-depth-and-guard-properties.md) — owner directive 2026-08-01: classify every review finding LOCAL vs FOUNDATIONAL BEFORE fixing it; foundational ⇒ root item + issue + labelled containment, never a patch in place (#1539). Plus the two guard properties invisible in negative cases — leaves correct work alone SILENTLY, and refuses what it cannot read (#1543) — and helper limits re-judged at each consumer (#1541). Measured: 88% FP rate on one guard; a gate whose mutation step threw for its entire life (12 CI runs, 0 verdicts); ~8 accidental-greens from writing the fix before the test.
- [Self-improving harness north-star](self-improving-harness-northstar.md) — robota ALWAYS runs the correct process inside an automated harness that SELF-IMPROVES to make robota continuously better; control loop SENSE(measure firing) → ENFORCE(mechanical dispatch) → IMPROVE(compounding lesson loop); vehicle = epic HARNESS-017 (type: INFRA)
- [No-Fallback mechanical gate (HARNESS-028)](no-fallback-gate.md) — DONE (PR #1216): three-layer No-Fallback enforcement = spec-doc `## Fallback & Degradation Declaration` (review) + `scan-no-fallback.mjs` floor (v1: `catch`→bare-default-literal, no throw; `allow-fallback: <reason>` suppression; string/comment-aware; excludes `.catch()`) + reason-less anti-rot. Narrowed to swallow→default (excludes blessed error-RESULT returns); ~11 sanctioned sites stamped, not a sweep. Deferred: `f()||g()`, stale-detection, `apps/` scope.
- [Harness reliability is mechanical, not skill-tree depth](harness-mechanical-not-skilltree.md) — dispatch/gate reliability comes from mechanical hooks + measurement, NOT a router→orchestrator→leaf skill tree (skills are agent-invoked prose; a router skill is prose-guessing +1 hop); independently endorsed by architecture-auditor + proposal-reviewer (2026-07-16)
- [A wait is not idle time](a-wait-is-not-idle-time.md) — owner directive (2026-08-05). Four pull requests were run strictly one after another; a review round costs 8-10 minutes and each took 3-6 rounds, so **over two hours of the session was spent blocked while 71 independent items were open**. The shape worth remembering: every individual decision to wait was defensible, and repeating it made the total indefensible — a cost only visible from outside, which is where it was seen. Rule OWNED by `.agents/rules/operational.md` § "A Wait Is Not Idle Time"; procedure by `worktree-parallel-orchestration`. "Independent" is stricter than the backlog numbering: two items touching the same ratchet or registry are one item
- [Accepting is not reading](wiring-tests-assert-the-wrong-half.md) — measured 2026-08-20/21: threading a value through layers, the easy test asserts the receiving side ACCEPTS it while the defect is that it never READS it. **Three instances in three consecutive issues, two caught by automated PR review rather than by me** — and the second and third came AFTER I had written the principle into the first one's own commit message. Accepting is not reading, existing is not producing, being called is not working. The check: name the observable the defect would change; if nothing can see it, the seam IS the work (`buildServeSessionOptions`, `presetSessionFields`, `currentPermissionRules()` were each created for this and each immediately caught a real defect)
- [A conditional spread hides a misplaced key](conditional-spread-hides-a-misplaced-key.md) — `exactOptionalPropertyTypes` makes `...(x !== undefined ? { k: v } : {})` the idiom for an optional key, and **excess-property checking does not apply to it** — a spread result is not an object literal, so a key on the wrong object compiles silently. Verified with a probe: written directly the same key errors `TS2353`. Measured from issue #1949: 356 sites in shipped src, 22 exposed (nested inside an object that is a property value), and **none of the 22 misplaced** — the gap is real and latent. A `defined()` helper does NOT restore the check (measured); annotating the spread SOURCE does, at a composition boundary. Found by review on pull request #1946, where both the compiler and the tests stayed green
- [Check validity has two axes; falsify guards](check-validity-two-axes.md) — a check has TWO independent properties: **can it fail** and **does it check the RIGHT thing**. Green on the first is not the second: `security audit` named a discipline and ran a dep scan (renamed `dependency audit`, INFRA-060 D5), `agent-server-boundary` is satisfied **vacuously by a never-called import**, the release workflow succeeded at _uploading_ an unopenable macOS artifact for four months. **Falsify — a green run is not evidence**: a newly written guard shipped containing the very defect it audited **three times in one session** (one had 3 defects, two masking each other). **Over-checking gets suppressed** — slug-equality would fire on 34 of 111 correct pairs. **A partial read looks complete** — unpaginated alerts returned `0 high` while 40 sat on page 2, a false all-clear I reported to the owner. **IDs claimed at authoring time collide** — 4 in one day (→ PROC-002). Ceiling: none of this catches a check whose _logic_ is wrong; ~30 of the 76-scan suite is measured vacuous (2026-07-26)
- [A cancelled CI job reads as a failed one](ci-cancelled-reads-as-failed.md) — editing a PR BODY retriggers CI (`pull_request` includes `edited`) and the concurrency group cancels the in-flight run; `gh pr checks` then renders those cancelled jobs as **fail** with plausible durations. Measured on #1588: 5 jobs, 0 failed steps. Ask `gh run view --json jobs` for the conclusion before diagnosing a red, and finish the description before the final push
- Enforcement architecture (owner: [`../rules/enforcement-architecture.md`](../rules/enforcement-architecture.md)) — every enforced process = worker (produces) / guardian (judges, emits verdict) / orchestrator (routes+rewinds); guardian MUST have a scan/hook floor; loop-back is hybrid (auto-re-drive for completeness gates, halt for human-decision gates); no skill-tree tiers. Owner decision 2026-07-16.
- Branch hygiene + delete-after-merge (owner: [`../rules/git-branch.md`](../rules/git-branch.md)) — cut feature branches ONLY from freshly-fetched `origin/develop`, never from a local (possibly squash-merged) branch (→ DIRTY merge); NEVER delete a remote branch until its PR is confirmed MERGED (a blind delete after a failed `gh pr merge` orphans the PR). Both enforced by `branch-guard.sh`. Incident + fix 2026-07-16. **A PR to `main` may ONLY come from `develop`/`release/*`/`hotfix/*`, NEVER a feature branch** — a feature→main PR skips integration + sweeps the whole develop delta onto main, diverging the branches (the #1216 incident 2026-07-18: main↔develop split at #1171, reconciled by a develop→main promotion). Now MECHANICALLY enforced by the `main-pr-source-guard` CI job (`.github/workflows/ci.yml`), not just the rule. Flow is fixed: feature→develop→main.
- Orchestration map (owner: [`../specs/orchestration-map.md`](../specs/orchestration-map.md)) — single at-a-glance registry of every orchestrator/worker/guardian pipeline (spec-gate, arch-refresh, doc-refresh, capability-extraction, PR-review, backlog-execution) + agent roster/signals; mechanically kept current by `scan-orchestration-map.mjs` (every `.claude/agents/*.md` must be listed). Update it in the same change as any agent/orchestrator change. Owner request 2026-07-16.
- Research is default-on + guarded (owner: [`../rules/research.md`](../rules/research.md)) — every draft/todo/active spec MUST carry a substantiated `## Prior Art Research` section or an explicit `Waived: <reason>`; worker = `prior-art-researcher` agent, guardian = backlog-gate-guard GATE-WRITE, floor = `scan-spec-research.mjs`. Owner requirement 2026-07-16.
- Agent-run capability verification (owner: [`../rules/backlog-execution.md`](../rules/backlog-execution.md)) — a user-facing capability (memory/retrieval/tools) is NOT done on unit tests: the spec/backlog PLAN must require it be reachable via a product surface AND verified by an AGENT-RUN e2e scenario the agent performs itself; a library seam no surface enables MUST NOT mark the user-execution gate "N/A". Trigger: SELFHOST-008 P2/P3/P4 shipped OFF in the real agent, never agent-run-verified (owner 2026-07-18). See [`agent-run-capability-verification.md`](agent-run-capability-verification.md).
- [CORE-027 failure contract](core-027-failure-contract.md) — DONE (#1676, merged develop 2026-08-09). A provider/tool failure now survives to the run result BY IDENTITY (new `execution-failure.ts`: `resolveProviderFailureError`+`buildFinalResult`; `IExecutionRoundState.providerFailure` carried via `onProviderFailure`) instead of being rendered to prose and re-parsed; dag one-shape-per-outcome, named DLQ-unsupported error, http node reads its own timeout signal. Abort-prose integration test is SCENARIO coverage (passes on develop too via the earlier `isAbortFailure` fix); identity red-proofs live in the agent-core units. Task archived to `completed/`.
- CORE-026 floating-promise/race repair (owner: [`../rules/code-quality.md`](../rules/code-quality.md)) — DONE (#1262). Turn double-start race fixed by claiming `executing` SYNCHRONOUSLY at `executePrompt` entry (before the checkAndRefresh await); wake/init floating rejections → `reportBackgroundError` (framework layer only — one-way dep); headless slash-command exit-code (`.catch` + print-mode `process.exit` outside try); `no-floating-promises` enabled on 3 packages (rollout → INFRA-040). RUNTIME-21/24 dropped. LESSON: a race/leak test asserting a LATE invariant is accidental-green — prove it FAILS pre-fix (recurred after ARCH-004's RUNTIME-14 test). See [`core-026-floating-promise-races.md`](core-026-floating-promise-races.md).
- HARNESS-041 accidental-green floor (owner: [`../rules/tdd-and-planning.md`](../rules/tdd-and-planning.md)) — DONE (#1272). Mechanical CI floor: for a `fix:` PR with a same-package (source+test) pair, reverse-apply the source hunks and require the changed test to FAIL (no rebuild — vitest reads `src`). C1 run-error!=pass; advisory rollout. Surfaced INFRA-044 (18 pre-existing dep advisories). See [`harness-041-accidental-green-floor.md`](harness-041-accidental-green-floor.md).
- CLI-061 CJK-IME defer-submit (owner: [`../rules/frontend.md`](../rules/frontend.md)) — DONE (#1269). Korean-IME last-char drop fixed at the APP LAYER (defer submit 50ms + re-read live `stateRef`; guard gates only a 2nd submit, input pipeline stays live). Terminal raw-mode race shared by all Node TUIs — Ink kept, migration rejected. See [`cli-061-cjk-ime-defer-submit.md`](cli-061-cjk-ime-defer-submit.md).
- ARCH-004 transport hardening (owner: forward-provisioned surface rule) — DONE (#1260). WS `stop()` no longer hangs (close(1001) each client → 5s terminate deadline → server close); HTTP `/submit` SSE listener leak fixed (`try/finally` + `stream.onAbort`→`session.abort()` + awaited/caught writes); concurrent `/submit` → `409` on `isExecuting()`; RUNTIME-54 subsumed by SEC-001 connect-time auth; STRUCT-07 pass-through removed. Follow-up STRUCT-08. See [`arch-004-transport-hardening.md`](arch-004-transport-hardening.md).
- CORE-025 permissionPolicy enforcement (owner: [`../rules/backlog-execution.md`](../rules/backlog-execution.md)) — DONE (#1258). The dead required `permissionPolicy` field (deny/preapproved/prompt/inherit-allowlist) is now ENFORCED at `PermissionEnforcer.checkPermission` BEFORE the mode gate — so deny/preapproved override even `bypassPermissions` (the `auto`-branch hole). Pure `resolvePermissionByPolicy` in agent-core; plumbed through the previously-dropped `ISubagentSpawnRequest`/`toSubagentStartRequest` path + both subagent runners. See [`core-025-permission-policy-enforcement.md`](core-025-permission-policy-enforcement.md).
- HARNESS-030 capability-reachability floor (owner: [`../rules/backlog-execution.md`](../rules/backlog-execution.md)) — DONE (#1255/#1256). Mechanizes the DECLARED half of the agent-run capability gate: `scan-capability-reachability.mjs` (in `run-all-scans`) fails a `.agents/spec-docs/done/` spec that declares `capability: true` but dodges agent-run evidence. Opt-in 3-key frontmatter — `capability: true` + `user_execution: agent-run|manual|none` + `user_execution_scenario: <explicit path>` (not a name guess; SEC-001's evidence is the GUI-007 scenario). Fences the SELFHOST-008 "declared-then-dodge" recurrence. See [`harness-030-capability-reachability-floor.md`](harness-030-capability-reachability-floor.md).
- No test doubles in shipped code (owner: [`../rules/code-quality.md`](../rules/code-quality.md)) — fake/mock/stub name TEST doubles only; a Fake*/Mock*/Stub* decl/factory must not be in non-test `packages/*/src`. Enforced by `scan-no-fake-in-src.mjs`(HARNESS-032,`allow-fake: <reason>`suppression). Fix = rename to InMemory/Manual/Recording/Scripted or move under a`testing/` subpath. Owner governance 2026-07-19 (recurred into SELFHOST-010). Pre-existing debt → HARNESS-033. See [`no-fake-in-src.md`](no-fake-in-src.md).
- Web-surface reorg + loopback security (owner-steered 2026-07-19: [`web-surface-and-sec001.md`](web-surface-and-sec001.md)) — GUI-007 (approved) dissolves `apps/agent-web-monitor`: CLI monitor SPA → `packages/agent-cli-web` (CLI-owned product-shell, CLI serves it from localhost — the deployed agent-web `/monitor` was the SEC-001 hole), hosted remote → `apps/agent-web` `/remote`; `agent-transport-gui` keeps its transport-family name. SEC-001 (approved) auto-mints the loopback WS token delivered to that CLI-served monitor. Distribution = single engine + layered CLI/GUI bundles (GUI .app ships a `robota` shim). Owner impl order: GUI-007 P1 (P1a done on branch) → SEC-001 → GUI-007 P2/P3.
- [Run the gate before you reach it](run-the-gate-before-you-reach-it.md) — the THIRD invariant axis: local reachability. `ci-mirror-map` pins local-vs-`protect-develop`; `scan-main-required-checks` pins can-it-fail; neither asked whether a required check is runnable before you reach it. Enforced for `protect-main`'s gate in `promote.mjs` + `promotion-preflight-parity`.
- [Bound every wait, and solve it yourself](bound-every-wait-and-solve-it-yourself.md) — owner directives 2026-08-16. An unbounded wait is what the owner sees as "stuck": bound anything long, judge success by the observable effect (`git ls-remote`) not an exit code, never re-run a command that already failed the same way, and never hand a tooling blocker back as a menu of options. The correct resolution here was `branch-guard`'s own rule — a check that is unrunnable or fires on correct work gets FIXED, not bypassed.
- [The Recommendation Gate is bounded at 2 revisions](recommendation-gate-has-a-two-round-bound.md) — observed 2026-08-16 on RULE-013 WU-B: the gate ran **11 rounds**. `backlog-execution-orchestrator/SKILL.md` states the bound twice and it never fired, because `no-progress` only catches a STUCK loop while `bound=2` exists to catch a **productive but unbounded** one. Every round after the third was a record correction the owner could have accepted or deferred — continuing silently answered a question that was theirs. Count the rounds; hand over on the third.
- [A claim true when written, not re-derived when the facts moved](claims-not-rederived-after-facts-moved.md) — four of five ARCH-025 review rounds plus one ARCH-030 round turned on ONE mode: an assertion checked once and never re-checked after something under it changed. Worst two shapes: a verification that cannot fail on the condition it names (converts "unverified" into "verified"), and a stale conclusion with a named downstream consumer (a wrong instruction, not just a wrong sentence). Before submitting, re-derive every assertion of consequence and state what would make each proposed check FAIL. Distinct from [`claimed-without-reading-back.md`](claimed-without-reading-back.md), which is about citing a measurement of a different tree.
- [A `#N` says whether it is an issue or a pull request](reference-kind-is-named.md) — owner directive 2026-08-19. Rule OWNED by [`../rules/naming-style.md`](../rules/naming-style.md) § "Reference Kind Is Named"; this file records why the tree-side check had to be a RATCHET (2,500 references across 443 files — a flat gate is red on arrival, and a check that is red on arrival gets suppressed rather than obeyed) while the commitlint side needed none (a check judging the artefact being WRITTEN is green on arrival by construction). Plus the `Closes #N` exemption the promotion machinery depends on, and three measured traps: `$` under `/m` closes a fence on its own first line, a ratchet count that fell to zero reads as a deleted file, and two custom commitlint rules must be ONE plugin object.
- [One table, two regex engines](one-table-two-regex-engines.md) — issue #1957. `payload-language-hazards.tsv` is compiled by the scan with `new RegExp` and matched by `bulk-edit-guard.sh` with `grep -E`, and the two do not read it the same way: grep interprets neither `\t` nor `\n` (inside a bracket they are two literal characters), and it matches one line at a time. A real newline cannot go in a grep pattern either — grep reads one as a SEPARATOR between alternative patterns. The fix folds the newline out of both the pattern and the payload onto a sentinel. Also: verify a regex claim under **bash**, not zsh — zsh expands the escapes in the variable, so the pattern tested is not the pattern the hook uses.
- [Ask the server, not the docs page](ask-the-server-not-the-docs-page.md) — issue #1930. A vendor's prose docs page and its `/.well-known/openid-configuration` gave OPPOSITE wrong answers about the same capability: the page said the device grant does not exist, discovery said it does. The server settled it — dynamic registration returns **201 without authentication** and `device_code` is **silently dropped** from the granted `grant_types`, surfacing later as `401 unauthorized_client`. The check that mattered: compare the REQUESTED field against the GRANTED one, because a 201 reads as success. Two siblings: an empty `content` was a reasoning-token budget, not "unsupported"; and the served model id can differ from the requested one, so key on `response.model`.
- [A report states what it could not see](a-report-states-what-it-could-not-see.md) — five ways a measurement came back confidently wrong in one session, caught every time by counting the subject rather than re-reading the instrument; the fifth has no better instrument, only asking. Owner: [`../rules/common-mistakes.md`](../rules/common-mistakes.md) 93–94
- [The owner's user-facing language is Korean, pinned](owner-user-facing-language-korean.md) — owner instruction 2026-08-23; the Language Policy matches language per-message and explicitly does not pin, so an agent reading only the rule reverts to English. Repo docs, commits, PR/issue bodies and peer-session messages stay English.
- [A scripted edit that rebuilds a region destroys what else was there](a-scripted-edit-that-rebuilds-a-region-destroys-what-else-was-there.md) — reconstructing a region silently drops what the template did not know about
- [A check's value is not bounded by what it was built to look for](a-checks-value-is-not-bounded-by-what-it-was-built-to-look-for.md) — the trip is worth more than the finding you dismissed
- [A check that reads the file matches the prose that quotes the code](applied-check-must-read-the-code-line.md) — measured 2026-08-23, both directions from one root: a mutation applied-check matched the docstring quoting its own regex and reported `NOT APPLIED` while 23 tests were red; and a scan matched a comment mentioning `hookResult.blocked`, so deleting the entire gate left it **exit 0**. Prose that explains an implementation satisfies the pattern that looks for it, and a single "does it still pass" probe finds only the second. Blank comments AND string/regex literals before matching; the priority corpus is files that document their own behaviour, and it is not only `scan-*.mjs`. Issue #2258
- [A corpus that is too wide makes a guard pass quietly](a-wide-corpus-makes-a-guard-pass-quietly.md) — measured 2026-08-23: a fire-site finder excluded `__tests__` but not `examples/`, so a demo script vouched for a production gate that could be deleted entirely with the scan still green. Too narrow fails loudly and gets investigated; too wide passes quietly and a higher count reads as healthier coverage. Require product source POSITIVELY rather than excluding known-bad trees — an exclusion list is a denylist and a new sibling directory should have to earn inclusion
- [Enumerating a sink is not covering it](enumerating-a-sink-is-not-covering-it.md) — measured 2026-08-23 on SEC-019: the sink enumeration was correct and the fix still shipped unguarded paths, because a sink is a VALUE and a path is where that value is produced. Asking who else obtains the value found three more sites including one carrying a model-chosen shell command. Put the treatment where the value is MADE so a new caller cannot obtain it unguarded; if two constructors produce it and share nothing, that is the finding. Assert the OUTPUT of a real render with poisoned input — "the sanitize call is present" is satisfied by a file containing one call
- [Two disagreeing measurements are one finding](two-disagreeing-measurements-are-one-finding.md) — measured 2026-08-23, five instances in one day: 17 fire sites vs 13 (the four extra were `examples/` vouching for deletable production code), six entangled types that were comment mentions, a PR green against its old base and broken against its new one, a `--max-warnings` cap read from a stale branch while its count was freshly measured, and a file count wrong three times by three methods. A cap and a count are a pair — verifying one operand lets the unverified one decide. Reconcile before recording a second count; if it cannot be reconciled, the gap IS the deliverable
- [A gate block notice is not the review](gate-block-notice-is-not-the-review.md) — measured 2026-08-23 on PR #2212: `review-gate: BLOCKED` and the reviewer's SHOULD finding landed two seconds apart, and reading the first as the round's output silently dropped the second, which had to be restated a round later. Enumerate every comment added since the previous round by timestamp rather than reading the last one — a body with no `ACTIONABLE FINDINGS:` line is not a review, and one that has it is not a gate notice
- [The author does not write the findings count](author-must-not-write-the-findings-count.md) — measured 2026-08-23: `ACTIONABLE FINDINGS: <n>` is the reviewer's verdict format and `pre-push-check.sh` reads the latest review-shaped comment, so an author reply carrying the count makes the hook read the PR as merge-ready and refuse the next push, including one fixing a red CI gate. Say each finding is addressed and what changed; leave the count to the reviewer
- [A sweep cannot tell a fixture from an assertion](a-sweep-cannot-tell-a-fixture-from-an-assertion.md) — patching the assertion always goes green and stops checking
- [A verification line is not evidence the verification ran](a-verification-line-is-not-a-verification.md) — SEC-016: my verification block reported `scenario:verify=0` for several turns via `pnpm scenario:verify` at the repo ROOT, where no such script exists; it is a package-level script in nine packages, so the command ran zero scenarios and `=0` was the absence of a gate, not a passing one. Caught only because it began returning 1 instead of 0 — an always-0 line is equally consistent with "always passes" and "never runs". A phantom gate is worse than none: it displaces the real one in the reader's accounting and buys confidence nothing earned. No repo defect — `pnpm harness:verify` already resolves the right scope and pre-push runs it. Read the command out of `pnpm run`; never compose one from a name that looks right.
- [Two measurements that disagree are one unresolved finding](two-measurements-that-disagree.md) — SEC-016: a spec said 13 fire sites, the scan it shipped with said 17, both written in the same change and neither reconciled. The 4-site delta WAS the defect — a demo script counted as evidence that the production gate it demonstrates exists, so deleting the gate left the scan green. The disagreement pointed the reassuring way, because a guard reporting a LARGER population reads as broader coverage — though the danger follows the direction of the CLAIM: extra corpus suppresses a PRESENCE proof (believed false negative) and only manufactures work for an ABSENCE proof. Resolve the gap before either number is used; writing both down is deciding not to look. Issue #2227 ALSO records its own limit: four commits after writing this file I shipped the identical defect (a SPEC row saying 13 where two other lines said 16), so a memory entry is a record that a lesson was learned, not a mechanism that applies it.
