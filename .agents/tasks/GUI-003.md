# GUI-003 — agent-app Electron packaging + bundled runtime

Spec: `.agents/spec-docs/active/GUI-003-agent-app-packaging-bundled-runtime.md` (status: in-progress).
Owner approved 2026-07-13 (rule-conformance delegated; proposal-review ENDORSE + REVISE applied). Depends on
RUNTIME-001 + DIST-001 (both done). Follow-up: RUNTIME-002 (headless-only Bun entry).

Package `apps/agent-app` per-OS with electron-builder, bundling the DIST-001 Bun `robota` binary via
`extraResources`; `sidecar.ts` resolves it from `process.resourcesPath` when packaged. Unsigned; signing deferred.

## Tasks

- [x] T1: electron-builder config (`electron-builder.yml`) + `dist:app` + `bundle-runtime.mjs` (host-arch →
      `resources-bin/robota[.exe]`). Linux AppImage proven locally with the bundled runtime.
- [x] T2: `sidecar.ts` `resolveSidecarCommand` (packaged/dev) + `main.ts` wiring; `sidecar.test.ts` 12/12.
- [x] T3: `release-desktop-app.yml` (OS matrix, concurrency, idempotent gh upload); `actionlint` clean.
- [x] T4: packaged-app e2e `bundled-runtime-e2e.mjs` against the BUNDLED runtime — nonce handshake + wrong-token
      reject + clean shutdown (TC-02a/TC-04) 3/3.
- [ ] T5: feature→develop→main via merge-verifier.
- [ ] T6 (GATE-COMPLETE): User Execution Test — dispatch `release-desktop-app.yml` on a real/draft tag; confirm
      installers attach to the Release (owner-gated — first public Release).

## Test Plan

- **Agent-owned (Linux, local):** `dist:app` → AppImage + bundled `resources/robota` (TC-01); `test:e2e:bundled`
  proves the bundled runtime's nonce handshake / reject / shutdown (TC-02a, TC-04); `sidecar.test.ts` both
  resolution branches (TC-03).
- **Static:** `actionlint` on the workflow; `harness:scan` 47/47; typecheck + lint (`--max-warnings 0`) clean;
  `release.yml`/`release-bun-binaries.yml` unchanged; no `agent-framework`/`agent-core` dep (TC-05, TC-06).
- **CI-only (mac/win + deb):** the matrix workflow produces dmg/zip + nsis on their runners and the `.deb` on
  ubuntu (needs `ar`, absent in the local sandbox) — verifiable only in CI on a dispatch/tag.
