# TASK-BL-001-agent-cli-logo-color-change

## Description

Change the color of the agent-cli logo. This is a visual/branding task for the CLI tool.

## Status

todo

## Context

Part of UI/UX refinement for the agent-cli.

## Test Plan

When this task is implemented, verify the CLI logo rendering in a headless CLI run and capture the rendered output for both the default terminal theme and a dark terminal theme. Also run the affected agent-cli package checks so formatting, linting, typecheck, and tests cover the visual code path.
