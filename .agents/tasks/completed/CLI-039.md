# CLI-039: init-command.ts Claude Code 설정 파일 JSON 파싱 보호

## Status: in-progress

## Objective

Guard `readClaudeSettings()` with try/catch, return `null` on parse failure, and output a "could not be parsed" warning message in the caller.

## Plan

1. Change `readClaudeSettings` return type to `Record<string, unknown> | null`
2. Return `null` on `JSON.parse` failure (remove allow-fallback comment)
3. In `runInitCommand`, check for `null` and output warning, skip migration
4. Add unit tests covering TC-01, TC-02, TC-03

## Affected Files

- `packages/agent-cli/src/init/init-command.ts`
- `packages/agent-cli/src/__tests__/init-command.test.ts` (new)

## Evidence

- GATE-IMPLEMENT PASS — 2026-05-25
