# CMD-005 — model-invocable `AskUserQuestion` tool on the CMD-004 ask seam

Spec: .agents/spec-docs/active/CMD-005-model-question-tool.md
Status: completed — GATE-VERIFY + GATE-COMPLETE passed 2026-07-02 (evidence in spec Evidence Log;
live TUI run: real claude-sonnet-4-6 invoked the tool, dialog rendered mid-turn, Enter answered,
model confirmed the pick). Approved decisions: D1 `AskUserQuestion`; D2 1–4 question batch; D3 default
built-in, permission-free; D4 headless → structured `unavailable` result.

## Decision (recorded)

- Tool lives in `packages/agent-tools/src/builtins/ask-user-question-tool.ts` (sibling of shell-tool);
  consumes `IToolExecutionContext.ask` (CMD-004 port) — no new taxonomy, maps onto `IActionRequest`.
- Context injection reuses the per-call context assembly seam (same route as `eventService`).
- Command-path model-invocation guard (`user-interaction-port.ts`) unchanged — the tool is THE model path.
- Cancellation/unavailable are data in the tool result, never thrown errors.

## Phases

### Phase A — the tool (agent-tools)

- [x] `ask-user-question-tool.ts`: Zod schema (questions 1–4: question/header/options/multiSelect/
      allowFreeText) → per-question `IActionRequest` (value = label, `ask_<uuid>` ids), sequential
      `context.ask` calls, JSON array result; `{ cancelled: true }` per cancelled question;
      `{ unavailable: true, reason }` when `context.ask` undefined.
- [x] Unit tests over a stubbed `context.ask`: single/multi/free-text mapping, batch order, cancelled,
      unavailable, schema bounds (0 or >4 questions rejected). (TC-01, TC-04 unit half)

### Phase B — context injection (framework/session)

- [x] Inject the session askHandler into per-tool-call `IToolExecutionContext.ask` (interactive only;
      headless stays absent).
- [x] TEST-003 functional test: scripted session; model turn calls AskUserQuestion; answer pre-queued
      via the programmatic path; assert the tool result carries the answer. (TC-02)

### Phase C — assembly + docs

- [x] `create-tools.ts` registration + `DEFAULT_TOOL_DESCRIPTIONS` entry.
- [x] 3-layer docs: agent-tools SPEC/README + content/ guide mention. (TC-05 docs part)

### Phase D — E2E + gates

- [x] PTY test (tui-e2e gate): real binary; model asks; drive the Ink dialog by keys; assert the
      model's final text reflects the picked answer. (TC-03)
- [x] Headless integration: print-mode run → tool resolves `unavailable`, no hang. (TC-04)
- [x] typecheck / lint / affected suites / `pnpm harness:scan` green. (TC-05)

## Test Plan / 검증

Spec `## Test Plan`이 SSOT. TC-01 unit(agent-tools, stubbed context.ask: 매핑/배칭/취소/unavailable),
TC-02 functional(TEST-003 scripted session + askHandler로 실제 InteractiveSession 구동), TC-03 PTY
E2E(실 바이너리 replay 픽스처, dialog 키 조작 → 답이 모델로 복귀), TC-04 headless(print mode 무정지 +
unavailable), TC-05 typecheck/lint/스위트/harness:scan green. 모든 TC는 자동 테스트로 커버(수동 행 없음).

## TC Coverage Map

| TC    | Covered by        |
| ----- | ----------------- |
| TC-01 | Phase A           |
| TC-02 | Phase B           |
| TC-03 | Phase D PTY       |
| TC-04 | Phase A + Phase D |
| TC-05 | Phase C + Phase D |
