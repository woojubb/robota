# CLI-030: Bash 권한 피로 해소 — 세션-레벨 "이 세션에서 항상 허용"

## Status: in-progress

## Objective

Add `[s]` shortcut key for "Allow for this session" in TUI permission prompt, and display `[s]` hint in the prompt options.

## Plan

1. Update `PERMISSION_PROMPT_OPTIONS[1]` in `permission-prompt-flow.ts` to include `[s]` hint
2. Add `s` as a shortcut key for index 1 in `getPermissionPromptInputAction`
3. Add unit tests covering TC-03 ([s] option shown) and TC-04 (allow-once doesn't add to session)

## Affected Files

- `packages/agent-transport/src/tui/flows/permission-prompt-flow.ts`
- `packages/agent-transport/src/tui/__tests__/confirm-permission-flow.test.ts`

## Evidence

- GATE-IMPLEMENT PASS — 2026-05-25
