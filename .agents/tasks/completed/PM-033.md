# PM-033: robota init 완료 후 프로바이더 설정 인라인 연결

## Status: in-progress

## Objective

After `robota init` completes, show Y/n prompt to run provider setup inline.
Skip prompt when `CI=true` or `--yes` flag.

## Plan

1. Add `yes?: boolean` option to `runInitCommand` signature
2. After "Next steps" output, add Y/n prompt (skip if CI=true or yes=true)
3. If Y, import and call `runInteractiveProviderSetup` from provider-startup.ts
4. Wire `--yes` flag through `cli-args.ts` and `preflight.ts`
5. Add unit tests covering TC-01, TC-02, TC-03, TC-04

## Affected Files

- `packages/agent-cli/src/init/init-command.ts`
- `packages/agent-cli/src/startup/preflight.ts`
- `packages/agent-cli/src/utils/cli-args.ts`
- `packages/agent-cli/src/__tests__/init-command.test.ts` (new/extended)

## Evidence

- GATE-IMPLEMENT PASS — 2026-05-25
