# CMD-004 — Separate interaction ACTION from UI (task breakdown)

Spec: `.agents/spec-docs/done/CMD-004-command-action-ui-separation.md`
Status: completed — Phase 1 complete (PRs A–I + final deletion #887 + TC-09 #888 merged; TC-01..TC-09 evidence in spec Evidence Log). Phase 2 (WS + web-ui multi-env broadcast) is a separate future track tracked under the still-open `backlog/CMD-004-*` item, not here.

Phase 1, decomposed into 4 additive-then-delete PRs (each green typecheck + code-reviewed). Phase 2
(WS + web-ui + multi-env broadcast) is tracked separately under the spec.

## PR-A — action contract + ask port in `agent-core` (additive) → TC-01

- [x] Add `IActionOption`, `IActionRequest` (incl. `masked`/`allowEmpty`/`placeholder`/`maxVisible`),
      `TActionResponse`, `IUserInteraction` to `agent-core` (pure types, zero-dep preserved).
- [x] Add ergonomic constructors + `isConfirmed` helper (`confirmAction`/`selectAction`/`multiSelectAction`/`textAction`).
- [x] Add `ask?: IUserInteraction['ask']` to `IToolExecutionContext` (reachable by tool source / CMD-005).
- [x] Export all from `agent-core` public surface.
- [x] Type test (TC-01): assert `IActionRequest` field set + `IUserInteraction.ask` exported.
- [x] Update `agent-core` `docs/SPEC.md` (Public API Surface + Type Ownership).
- [x] Leave old contracts (`agent-interface-transport`) untouched — additive only; build/typecheck green.

## PR-B — wire `ask` + TUI renderer + migrate 6 simple commands → TC-04 (part), TC-05, TC-06 (part), TC-07

- [x] Inject `askHandler` into `InteractiveSession` (sibling to `permissionHandler`); thread through all
      session-creation sites (TUI channel, `createInteractiveRuntime`, headless, programmatic).
- [x] Expose `ask` to commands as a narrow capability port (REFACTOR-006 pattern, not on base context).
- [x] Model-invocation guard: `invocationSource === 'model'` → ask resolves `{ cancelled }` (TC-07).
- [x] Build the TUI `pendingAction` Ink renderer (single/multi-select/confirm/masked); wire abort →
      resolve-cancelled and input-gating while pending (TC-06).
- [x] programmatic + headless channels answer the unified action (TC-05).
- [x] Migrate the 6 Path-A commands (mode, preset, provider, language, exit, clear) to inline
      `context.ask` (TC-04 single-select + confirm).

## PR-C — migrate 13 provider wizard sites → TC-04 (masked + re-ask) ✅ merged (PR #886)

- [x] Convert `provider/*` returned-continuation wizards to sequential `await context.ask(...)`.
- [x] `masked` API-key entry; duplicate-name validation as a re-ask loop.
- [x] Tests: masked request + re-ask-on-invalid (TC-04); scripted `getUserInteraction()` double; absent-renderer coverage.
- Code-review gate passed; tests strengthened to assert confirm/step prompts are issued.

## PR-D — delete the old paths → TC-02, TC-03 ✅ done

- [x] Delete Path-A `interactionHints` + `buildActionRequest`/`resolveArgsFromResponse`.
- [x] Delete Path-B `ICommandResult.interaction`, `ICommandInteraction`, `TCommandInteractionPrompt`,
      `ICommandChoicePromptOption`, `InteractivePrompt`/useSideEffects interaction path, dead
      `CommandPicker`/`CommandConfirm`, and the dead `IPermissionRequest` event.
- [x] Removed legacy `requestAction`/`TActionRequest`(pick/confirm)/`TActionResponse`(legacy)/`IPickItem`/
      `TCommandInteractionHint`; promoted `askUser` to required on `IInteractionChannel`; replaced
      `IAgentDriver.queueAction` with `queueUserAction`. Three-doc-layer SPEC sync done.
- [x] Typecheck green across all packages; `rg` absence check (only historical comments remain);
      `harness:scan` → all 33 scans pass (TC-08).

## Final → TC-08, TC-09 ✅ done

- [x] `pnpm harness:scan` → 33 scans pass (TC-08).
- [x] User Execution evidence captured automatically (never-ask-the-user-to-test): real-binary PTY
      `provider-setup.ptytest.ts` drives `/provider add`, asserts the masked API-key field renders `*`
      and never echoes the secret, then Esc cancels. `test:pty` → 9 passed (TC-09).

## Test Plan / 검증

Each PR runs its package build + tests and `pnpm -w typecheck` before opening; every PR that touches
`.ts` passes the Pre-Merge Code-Review Gate (`/code-review`, all findings resolved) before merge. TC
mapping: PR-A→TC-01; PR-B→TC-05/06/07 + TC-04(part); PR-C→TC-04(masked/re-ask); PR-D→TC-02/03; final
gate TC-08 (`pnpm harness:scan`) and TC-09 (manual real-TUI scenario with recorded evidence). Type test
for the contract (TC-01), vitest unit/integration via a mock `IUserInteraction` (TC-04/05/07), and a PTY
test for TUI render incl. masked + abort (TC-06).
