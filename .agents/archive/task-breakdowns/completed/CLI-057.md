# CLI-057 Tasks — Grep count mode + headLimit

Spec: `.agents/spec-docs/active/CLI-057-grep-count-headlimit.md`

- [x] T1 (TC-03): schema — `count` enum value + `headLimit` positive-int param; description rewrite (camelCase headLimit, real modes only)
- [x] T2 (TC-01): searchFile count branch → `path:count` rows
- [x] T3 (TC-02): grepFileTool headLimit slice + `(+N more results truncated by headLimit)` marker
- [x] T4 (TC-04): regression tests — files_with_matches + content(contextLines) on temp fixture
- [x] T5 (TC-05): SPEC.md Grep row update (if listed); build + test green
