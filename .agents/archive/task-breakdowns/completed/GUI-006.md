# GUI-006 — web GUI unification over agent-transport-gui + absorb/retire agent-web-ui

Task file: [`.agents/spec-docs/active/GUI-006-web-gui-unification.md`](../spec-docs/active/GUI-006-web-gui-unification.md).
Owner-approved 2026-07-12 (verbatim: "Design A (권장)"). Placement independently proposal-reviewed (REVISE →
ENDORSED, 5 corrections applied). GUI Phase-2 of the GUI-005 taxonomy.

Design A: SessionMonitor → `agent-transport-gui` (core); browser-remote RTC peer → NEW
`packages/agent-transport-webrtc-web` (browser sibling of node `agent-transport-webrtc`); CLI-served Vite SPA →
NEW `apps/agent-web-monitor` (web sibling of `apps/agent-app`); delete `agent-web-ui`. Behavior-preserving.

## Tasks

- [x] T1: GATE-IMPLEMENT — task file + spec → active/, in-progress.
- [x] T2 (TC-02): Scaffold `packages/agent-transport-webrtc-web` (mirror the agent-web-ui package shape); MOVE
      the browser-remote peer from `agent-web-ui/src`: `client/rtc-session-client.ts`, `rtc-signaling.ts`,
      `rtc-responder-gate.ts`, `device-credential-store.ts`, `parse-ice-servers.ts`, `parse-remote-location.ts`,
      `hooks/useRtcSession.ts`, `components/RemoteClient.tsx`, and the `client/__tests__/*` (45 tests). Deps:
      `agent-transport-gui` + `agent-remote-pairing` + `agent-transport-protocol`; peer `react`. NO
      interface-transport (transitive), NO werift/ws, NO react-markdown. Build + typecheck + test.
- [x] T3 (TC-01): MOVE `SessionMonitor.tsx` into `packages/agent-transport-gui/src/components/`; export from
      `src/index.ts` (reachable via `./client`); update the core SPEC public-API table. Build + typecheck + test.
- [x] T4 (TC-03): Scaffold `apps/agent-web-monitor` (Vite, mirror `apps/agent-app` app shape); MOVE
      `agent-web-ui/spa/*` (`index.html`+monitor entry → core `SessionMonitor`; `remote.html`+remote entry →
      `agent-transport-webrtc-web` `RemoteClient`; `main.css` = Tailwind entry + `@source` over the core, like
      agent-app). Build; agent-owned Playwright smoke of both pages (monitor connects to a stub WS; remote
      mounts) + screenshots.
- [x] T5 (TC-04/TC-05): Re-point consumers — `agent-cli/scripts/copy-web-assets.mjs` source →
      `apps/agent-web-monitor/dist`; `agent-cli/package.json` build step
      `--filter @robota-sdk/agent-web-ui build:spa` → `--filter @robota-sdk/agent-web-monitor build`;
      `remote-control-controller.ts:138` help string → new Stage-D page home; `apps/agent-web`
      `MonitorClient.tsx` import → `@robota-sdk/agent-transport-gui/client` + drop the dep. Verify agent-cli
      build produces `dist/web/{index,remote}.html`; robota-web typecheck + build green.
- [x] T6 (TC-06): DELETE `packages/agent-web-ui`; grep no dangling refs (excl. CHANGELOG/historical spec-docs);
      update `.agents/project-structure.md` (inventory + Library Neutrality product-shell clause ~line 78),
      arch-map, `scripts/harness/check-capability-placement.mjs` (add agent-transport-webrtc-web +
      agent-web-monitor, remove agent-web-ui). Affected typecheck + tests + `pnpm harness:scan` all green;
      dependency-direction + no cycle; core still contract-pure. Independent conformance audit.
- [x] T7: feature→develop→main via merge-verifier; run the browser smoke myself (agent-owned).
- [x] T8: GATE-COMPLETE — spec active→done; archive task file.

## Test Plan

- **TC-01** (command): `pnpm --filter @robota-sdk/agent-transport-gui build && typecheck && test`; `SessionMonitor`
  exported + in the public-API table; core deps unchanged (contract/protocol only).
- **TC-02** (command): `pnpm --filter @robota-sdk/agent-transport-webrtc-web build && typecheck && test` (45 tests
  migrated); deps assertion — no interface-transport/werift/ws/react-markdown; no pass-through re-export.
- **TC-03** (headless browser, agent-owned): build `apps/agent-web-monitor`; Playwright loads `index.html`
  (monitor connects to a stub WS) + `remote.html` (RemoteClient mounts); screenshots captured + shown.
- **TC-04** (command/e2e): `pnpm --filter @robota-sdk/agent-cli build`; assert `dist/web/index.html` +
  `dist/web/remote.html` present; CLI web-serve/remote-control path behavior unchanged.
- **TC-05** (command): `pnpm --filter robota-web typecheck && build`; `/monitor` imports from
  `@robota-sdk/agent-transport-gui/client`.
- **TC-06** (harness/static): grep no `agent-web-ui` (excl. CHANGELOG/historical spec-docs); `pnpm harness:scan`
  all green; independent conformance audit; project-structure + arch-map + capability-placement updated.

## Completion (2026-07-12)

DONE — shipped to `main` (impl #1141 `7f13782a1` → promotion #1142 `00837ba25`), both hops merge-verified
(LANDED: PASS). Independent conformance audit: code side fully conformant; doc-side sweep applied. Verified
agent-owned: typechecks clean, tests gui 10 + webrtc-web 45 + agent-app 12, headless web smoke 2/2, CLI copy →
dist/web/{index,remote}.html, harness:scan 49/49. Spec → `.agents/spec-docs/done/`.
