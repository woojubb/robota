# Historical loop-run records

The JSONL files in this directory are immutable historical evidence produced by the retired
`loop-run` subsystem. Issue #2826 deleted its writer and judging scans; these records are not an
active workflow, input, receipt requirement, or compatibility surface. Do not add new entries.
