{
  "$schema-note": "INFRA-055. SOURCE OF TRUTH for which status checks `protect-main` must require. This file is the source, not a mirror: `scripts/harness/scan-main-required-checks.mjs` (registered in `pnpm harness:scan`) asserts offline that every context named here resolves to a workflow job that ACTUALLY RUNS AND CAN FAIL on a PR whose base is `main`, and `.github/workflows/ruleset-drift.yml` reconciles it against the live ruleset on a schedule. Change the ruleset and this file in the same change.",
  "why": "Measured on promotion #1427: all five contexts `protect-main` then required were no-ops on a `main` PR — `build`/`quality`/`scans`/`security audit` were 3-6 second echoes whose every real step was gated on `base_ref != 'main'`, and `commitlint` was skipped wholesale. Branch protection reported green from jobs that deliberately did no work, while the job that verifies a promotion (`release-grade verification`, 7m31s) was not required and CodeQL failed without blocking.",
  "branches": {
    "main": {
      "ruleset": "protect-main",
      "ruleset_id": 18715845,
      "required_status_checks": [
        {
          "context": "promotion ancestry",
          "workflow": ".github/workflows/ci.yml",
          "job": "promotion-ancestry",
          "verifies": "INFRA-051 A1/A2/A3 — the promotion carries `main`'s ancestry, adds no commit `develop` has never seen, and promotes `develop`'s tree unchanged."
        },
        {
          "context": "main PR source guard",
          "workflow": ".github/workflows/ci.yml",
          "job": "main-pr-source-guard",
          "verifies": "#1216 recurrence guard — a PR to `main` comes from `develop`/`release/*`/`hotfix/*` in this repository, never a feature branch and never a fork."
        },
        {
          "context": "release-grade verification",
          "workflow": ".github/workflows/ci.yml",
          "job": "release-grade-verify",
          "verifies": "`pnpm harness:verify:release` — the only required context that executes the repository's code on a promotion: full build, full scan suite, harness suite, package tests, binary e2e, typecheck, lint, plus an unconditional osv-scanner."
        }
      ],
      "deliberately_not_required": [
        {
          "context": "build / quality / scans / security audit",
          "reason": "Develop-side gates. On a `main` PR they are excluded at the job level and `release-grade verification` subsumes them: it runs the FULL build and the FULL scan/test/typecheck/lint sweep where these are affected-scope-selective via `--base-ref`, and its osv-scanner step is unconditional where `security audit`'s is gated on a manifest diff."
        },
        {
          "context": "commitlint",
          "reason": "Excluded on `main` by design — a promotion's merge-commit subject is generated by GitHub, and every commit it promotes was linted on its own PR to `develop`. It was previously REQUIRED here while skipping wholesale, which branch protection accepts."
        },
        {
          "context": "CodeQL / Analyze (javascript-typescript)",
          "reason": "`codeql.yml` carries `paths-ignore` for markdown/docs, so a docs-only promotion (routine in this repo) never triggers it and the context never reports — a required context that cannot report leaves the PR permanently pending, which is the #1436 review-gate rollback shape. Separately, the `CodeQL` results check is diff-scoped and a promotion's diff is the whole `develop`->`main` delta, so its verdict scales with promotion size rather than with anything the promotion decides (GitHub's own summary on #1427: `Alerts not introduced by this pull request might have been detected because the code changes were too large`). Follow-up: converge on `review-gate`, which fails closed and already shares `classify-changed-paths.mjs` with ci.yml, after it has been observed reporting a real conclusion on both a code and a docs-only promotion."
        }
      ]
    }
  }
}
