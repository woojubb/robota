---
'@robota-sdk/dag-framework': patch
---

Bound memory used by default regex text replacement when a global pattern has many matches, while preserving JavaScript replacement and UTF-8 byte-limit behavior.
