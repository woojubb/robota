#!/usr/bin/env sh
# work-run measurement (which drove this hook) was removed 2026-09-08 (§7, harness overhead
# report). This file must exist and stay executable: .husky/_/post-checkout fails closed
# when its tracked target is missing, by design, so it cannot be deleted outright.
exit 0
