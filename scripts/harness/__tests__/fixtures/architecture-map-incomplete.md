# Incomplete Architecture Map (fixture)

This fixture has a scope line but intentionally omits the up-link and any structure block,
so `check-architecture-map-completeness.mjs` must FAIL it (exit 1).
