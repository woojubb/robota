#!/usr/bin/env node

/**
 * Guard-scope fail-closed floor (HARNESS-052).
 *
 * THE DEFECT, one level up. Every gate this repository lost in the last week reported success over
 * ground it never covered — `Claude review` skipping and exiting 0, `scans` printing `SKIPPED … Not
 * a pass`, `protect-main`'s required contexts echoing for three seconds. The scans written to fence
 * those in are themselves written in the same idiom:
 *
 *     const dir = path.join(root, WORKFLOW_DIR);
 *     if (!existsSync(dir)) return [];        // ← "nothing to check" == "clean"
 *
 * MEASURED on this tree, by running each registered scan's finder against a root that does not
 * contain its governed tree: **30 of the 50 finders returned an empty finding list** — i.e. reported
 * a pass — including the three that guard `.github/workflows`, the directory whose contents are the
 * subject of five of the ten incidents. The 20 that fail closed are the proof the shape is
 * avoidable, not inherent.
 *
 * SINCE REPAIRED (HARNESS-052, second pass): 27 of those 30 now throw on their absent governed tree,
 * via the shared `requireGovernedTree` helper, and each was moved into `MANDATORY_TREE_GUARDS` only
 * after this scan MEASURED the change by execution — the ledger itself demanded every promotion,
 * one finding at a time. Four vacuous entries remain, each with its reason recorded below: two are
 * pure enumerators whose caller renders the verdict, one governs `node_modules` (not a checked-in
 * tree, so its absence is a legitimate state), and one is owned by another item.
 *
 * THE RULE, in three parts, because each alone can be satisfied vacuously:
 *
 *   1. CLASSIFICATION COMPLETENESS. Every scan registered in `run-all-scans.mjs` that exports a
 *      `find…`/`collect…` finder taking a `root` must appear in exactly one of the two tables below.
 *      The finder set is DERIVED from the registration list and the source — never hand-listed — so
 *      a new scan cannot be added without answering for its behaviour, and a table entry for a scan
 *      that no longer exists is itself a finding (anti-rot).
 *
 *   2. BEHAVIOUR. Every `MANDATORY_TREE_GUARDS` entry is EXECUTED against a temporary root that
 *      lacks its governed tree, and must throw or return at least one finding. This is a behavioural
 *      assertion, not a source-pattern match: it cannot be satisfied by rewording the code, and it
 *      cannot pass on a guard that merely looks fail-closed.
 *
 *   3. LEDGER FRESHNESS. Every `PENDING_CLASSIFICATION` entry's recorded `measured` value is
 *      RE-EXECUTED on every run and must still match. The ledger went stale within the hour it was
 *      written — repairing `scan-conflict-markers` turned it fail-closed while the ledger still said
 *      `vacuous` — and a debt ledger nobody re-measures is a set of claims about the past presented
 *      as facts about the present. An entry that was fixed must be moved out; one that regressed
 *      must be caught.
 *
 * THIS SCAN HAS ALREADY BEEN CAUGHT COMMITTING ITS OWN DEFECT, which is why rule 1's derivation is
 * spelled out so defensively. Its first version matched `export function (find…)\(\s*root\s*=` and
 * so derived 20 of the 50 finders: it saw neither `export async function`, nor a `collect…` finder,
 * nor a `root` parameter without a default — and therefore did not classify ITSELF, since
 * `findGuardScopeFindings` is async. Falsified by registering a scan exporting
 * `export async function findBogusFindings(root = X) { return []; }`, vacuous for every root: the
 * completeness rule passed. Flipping the single keyword `async function` → `function` made the same
 * file fail. A second defect masked it — `finder(bare)` was not awaited, so a classified async
 * finder would have been reported as violating no matter how it behaved. A third: the regex matched
 * the example declaration in this very docstring, deriving a finder for a function that does not
 * exist, so comments are now stripped before matching. And the first ledger was generated by a
 * throwaway `node -e` loop that scored an IMPORT CRASH as `fail-closed`; regeneration goes through
 * the exported `measureFinder`, the same path rule 3 verifies with.
 *
 * THE CEILING, stated rather than implied. `PENDING_CLASSIFICATION` is not an exemption list and
 * must not be read as one — every entry carries the verdict its finder actually produces, and the
 * four that still measure `vacuous` are live instances (or reasoned permanent exceptions) recorded
 * in HARNESS-052. The behavioural half covers only the guards listed in `MANDATORY_TREE_GUARDS`.
 * Anything outside the derived finder set — a scan that walks its tree inline in `main()`, or one
 * that takes no root — is invisible here. A pass from this scan means "the classified guards fail
 * closed and the ledger is current", never "no guard can be satisfied vacuously". It also says
 * NOTHING about whether a guard's rules are correct when its tree IS present, which is the whole of
 * this item's second axis.
 *
 * Exit code 0 = every classified guard behaves as declared, 1 = violation found.
 */

import { mkdtempSync, readFileSync, rmSync } from 'node:fs';
import { tmpdir } from 'node:os';
import path from 'node:path';
import { pathToFileURL } from 'node:url';

const WORKSPACE_ROOT = path.resolve(import.meta.dirname, '../..');
const HARNESS_DIR = path.join('scripts', 'harness');
const REGISTRATION_FILE = path.join(HARNESS_DIR, 'run-all-scans.mjs');

/**
 * Guards whose governed tree is MANDATORY in this repository: its absence means the scan is running
 * somewhere it cannot judge, which is an error and never a pass. Each must throw or report a finding
 * when handed a root without that tree.
 */
export const MANDATORY_TREE_GUARDS = [
  {
    file: 'scan-review-token-supply.mjs',
    finder: 'findReviewTokenSupplyFindings',
    tree: '.github/workflows',
    why: 'the token-supply rule exists because the review action skips and exits 0 when it authenticates via OIDC against a divergent workflow; with no workflow to inspect, "nothing to guard" is indistinguishable from the failure it guards (INFRA-062 replaced the parity scan here, carrying this classification with it)',
  },
  {
    file: 'scan-ci-base-history.mjs',
    finder: 'findBaseHistoryFindings',
    tree: '.github/workflows',
    why: 'a grafted `--depth` fetch made required jobs report `skipping`, which branch protection accepts — the guard reading zero workflows is the same hole reopened',
  },
  {
    // Measured on an empty root: reports R1 findings for every declared path that does not exist,
    // so an absent tree is an error rather than "no build tooling changed, nothing to check".
    file: 'scan-build-tooling-scope.mjs',
    finder: 'findBuildToolingScopeFindings',
    tree: 'the declared workspace-wide build-tooling paths',
    why: 'it pins that a build-tooling change selects the FULL workspace; over a tree where those paths are absent the claim is vacuously true, which is exactly the `scopes: []` green that D4 exists to close',
  },
  {
    // Measured 2026-07-26 against a bare root: reports `the shared resource ceiling is missing`
    // with `inspected: 0`, so an absent tree reads as a problem rather than as "no vitest config
    // here, nothing to check". The distinction matters because the property it pins — that every
    // config inherits the heap ceiling — is exactly the one whose absence caused an OOM.
    file: 'scan-vitest-resource-ceiling.mjs',
    finder: 'findCeilingFindings',
    tree: 'packages/ and apps/ vitest configs plus vitest.shared.ts',
    why: 'without the shared ceiling V8 grants each worker a RAM-derived heap limit (measured 4144 MB on a 23 GB host); over a tree with no configs that claim would be vacuous',
  },
  {
    file: 'scan-workflow-permissions.mjs',
    finder: 'findWorkflowPermissionFindings',
    tree: '.github/workflows',
    why: 'it pins that no workflow holds an unjustified `write` scope; over zero workflows that claim is vacuously true, which is precisely the permission widening it exists to surface',
  },
  {
    file: 'scan-action-references.mjs',
    finder: 'findActionReferenceFindings',
    tree: '.github/workflows',
    why: 'INFRA-059 — an unresolvable `uses:` kills the job before any step reports, so the run reads green; a resolvability guard that inspected no workflow would emit that same green over nothing, which is the failure it exists to end',
  },
  {
    file: 'scan-required-check-needs.mjs',
    finder: 'findRequiredCheckNeedsFindings',
    tree: '.github/required-status-checks.json',
    why: 'measured: throws `required-status-checks.json is missing` on an empty root. The dependency edges it asserts are quantified over the declared contexts, so reading none would certify the #1424 bypass as absent',
  },
  {
    file: 'scan-automerge-disarm-permission.mjs',
    finder: 'findAutomergePermissionFindings',
    tree: '.github/workflows',
    why: 'an unpermitted auto-merge disarm fails silently; a permission guard that inspects no workflow reproduces exactly that silence',
  },
  {
    file: 'scan-no-fallback.mjs',
    finder: 'findNoFallbackFindings',
    tree: 'packages',
    why: 'the No-Fallback floor governs shipped package source; a repository checkout without `packages/` is broken, not clean',
  },
  {
    file: 'scan-no-fake-in-src.mjs',
    finder: 'findFakeInSrc',
    tree: 'packages',
    why: 'the no-test-doubles-in-shipped-code floor governs shipped package source; same reasoning as no-fallback',
  },
  {
    file: 'scan-tautological-assertions.mjs',
    finder: 'findTautologicalAssertions',
    tree: 'packages, apps and scripts',
    why: 'a test-assertion floor that finds no test files has not found clean tests, it has found no tests',
  },
  {
    file: 'scan-conflict-markers.mjs',
    finder: 'findGitConflictMarkers',
    tree: 'packages, apps and scripts',
    why: 'merge debris in a tree that was never opened is the one thing a "conflict markers" gate must never report clean',
  },
  {
    file: 'scan-release-sweep-coverage.mjs',
    finder: 'findReleaseSweepCoverageFindings',
    tree: 'pnpm-workspace.yaml and the workspace manifests it names',
    why: 'MEASURED on a bare root before registration, not assumed: it throws `pnpm-workspace.yaml is missing` from `readWorkspaceGlobs`, deliberately, rather than enumerating zero manifests. That distinction is the whole point of the guard — it exists because `pnpm run -r --if-present test` reports success over every workspace it never looked at, and a coverage floor that discovered no test scripts would report exactly the same success over the same silence',
  },
  {
    file: 'scan-test-plan.mjs',
    finder: 'collectTestPlanFindings',
    tree: '.agents/spec-docs',
    why: 'every directory this scan reads was optional, so a root with none of them printed "test-plan scan passed" — "no planning documents were found" wearing the words "every planning document has a test plan". The spec-doc pipeline is mandatory here, so its absence is an error',
  },
  {
    file: 'scan-orchestration-neutrality.mjs',
    finder: 'findOrchestrationNeutralityFindings',
    tree: 'the configured orchestration contract directories',
    why: 'over a root without them, "no app-domain identity leaks into the neutral contracts" is a statement about no contracts. Its `root` was also DECORATIVE — the walker closed over the real workspace root, so every caller was handed a verdict about this repository no matter which root it asked about, the same defect this item fixed in scan-no-fallback',
  },
  {
    file: 'scan-memory-neutrality.mjs',
    finder: 'findMemoryNeutralityFindings',
    tree: 'packages',
    why: 'HARNESS-052 measured it VACUOUS — `if (!existsSync(packagesDir)) return findings` answered "no neutrality findings" for a checkout with no library source at all. The library tree is the whole subject of a library-neutrality floor: finding no library is not finding it neutral',
  },
  {
    file: 'scan-live-smoke-provider-coverage.mjs',
    finder: 'findUncoveredProviderCredentials',
    tree: '.github/workflows and packages',
    why: 'INFRA-061 measured this guard VACUOUS on the half-root case — workflow present, provider packages absent: zero declarations discovered, zero findings, green. It now reports `no-provider-declarations-found`, because a provider-coverage floor that found no providers has not found them all covered',
  },
  {
    file: 'check-architecture-map-paths.mjs',
    finder: 'findArchitectureMapPathFindings',
    tree: '.agents/specs/architecture-map',
    why: 'MEASURED vacuous on a bare root before the repair: the map corpus IS the subject, so "every cited path exists" over no map is a claim about nothing',
  },
  {
    file: 'check-backlog-placement.mjs',
    finder: 'findBacklogPlacementFindings',
    tree: '.agents/backlog and .agents/backlog/completed',
    why: 'placement is a claim about the backlog tree; with no tree there are no misplaced items and no correctly-placed ones either',
  },
  {
    file: 'check-backlog-placement.mjs',
    finder: 'findDuplicateIdFindings',
    tree: '.agents/backlog and .agents/backlog/completed',
    why: 'an ID collision is a relation between two directories — reading neither cannot establish that neither collides, and eight collisions landed in one week',
  },
  {
    file: 'check-command-layering.mjs',
    finder: 'findCommandLayeringFindings',
    tree: 'packages',
    why: 'the layering rule governs shipped command packages; a checkout without packages/ is broken, not clean',
  },
  {
    file: 'check-dep-kind.mjs',
    finder: 'findDevDepOnlyRuntimeImports',
    tree: 'packages and apps',
    why: 'it quantifies over every workspace package; over none, "no devDependency is imported at runtime" is vacuously true',
  },
  {
    file: 'check-done-evidence.mjs',
    finder: 'findDoneEvidenceFindings',
    tree: '.agents/backlog/completed',
    why: 'the completed-backlog tree is the evidence corpus, and a readdir failure was CAUGHT and returned as "no unearned done claims" — a swallowed error wearing a verdict',
  },
  {
    file: 'check-ghost-package-refs.mjs',
    finder: 'findGhostPackageRefFindings',
    tree: 'packages',
    why: 'a reference is a ghost RELATIVE to the workspace package set; with no packages/ the resolution corpus is empty and every token is unresolvable while none is reported',
  },
  {
    file: 'check-harness-config-paths.mjs',
    finder: 'findHarnessConfigPathFindings',
    tree: 'scripts/harness',
    why: 'the harness scripts are the documents whose quoted paths this scan validates; no scripts means no validation, not valid paths',
  },
  {
    file: 'check-orphan-exports.mjs',
    finder: 'findOrphanExportFindings',
    tree: 'packages',
    why: 'an orphan verdict is quantified over the workspace reference corpus; an empty corpus makes every export an orphan and reports none of them',
  },
  {
    file: 'check-publish-safety.mjs',
    finder: 'findPublishClaimFindings',
    tree: 'packages',
    why: 'it checks publish claims in package SPECs, and its own summary says "all publishable packages" — over no packages that sentence is the audited defect verbatim',
  },
  {
    file: 'check-sdk-public-surface.mjs',
    finder: 'findSdkPublicSurfaceFindings',
    tree: 'packages/agent-framework/src',
    why: 'the SDK source tree is the surface under audit; walking zero files reports a clean surface it never saw',
  },
  {
    file: 'check-spec-paths.mjs',
    finder: 'findSpecPathFindings',
    tree: 'packages',
    why: 'it validates the paths cited by package SPECs; with no packages/ there are no SPECs, and the pass means nothing was read',
  },
  {
    file: 'check-spec-public-surface.mjs',
    finder: 'collectUndocumentedExports',
    tree: 'packages',
    why: 'the documented-vs-exported comparison needs both sides; with no packages/ neither exists and the difference is empty by construction',
  },
  {
    file: 'check-spec-public-surface.mjs',
    finder: 'findPublicSurfaceFindings',
    tree: 'packages',
    why: 'same corpus as the collector above: an absent packages/ makes "every advertised identifier exists in src/" true of nothing',
  },
  {
    file: 'check-stub-markers.mjs',
    finder: 'findStubMarkerFindings',
    tree: 'packages',
    why: 'stub markers are searched in shipped package source; no packages/ means no search, not a clean search',
  },
  {
    file: 'check-task-archival.mjs',
    finder: 'findTaskArchivalFindings',
    tree: '.agents/tasks',
    why: 'the task tree is the subject, and a readdir failure was CAUGHT and returned as "nothing to archive"',
  },
  {
    file: 'check-temp-script-placement.mjs',
    finder: 'findParkedTempScripts',
    tree: 'packages and apps',
    why: 'it globs the two source families for parked temp scripts; over neither, "none parked" is a statement about an empty glob',
  },
  {
    file: 'check-workspace-refs.mjs',
    finder: 'findWorkspaceRefFindings',
    tree: 'packages',
    why: 'resolution is relative to the workspace package set; with none, every reference is unresolvable and none is reported',
  },
  {
    file: 'scan-api-pagination.mjs',
    finder: 'findUnpaginatedApiQueries',
    tree: 'packages and apps',
    why: 'the pagination floor governs shipped source; reading no source is not finding it paginated',
  },
  {
    file: 'scan-capability-reachability.mjs',
    finder: 'findCapabilityReachabilityFindings',
    tree: '.agents/spec-docs/done',
    why: 'declared capabilities live in completed spec documents; with none, the reachability claim covers no capability at all — and this floor exists precisely because a declared capability was dodged',
  },
  {
    file: 'scan-deployment-matrix.mjs',
    finder: 'findTransportNames',
    tree: 'packages',
    why: 'transport names are enumerated FROM the package tree; over an absent one the matrix reads as entirely phantom, which is a verdict about the reader rather than a measurement of the code',
  },
  {
    file: 'scan-deprecated-markers.mjs',
    finder: 'findDeprecatedMarkerFindings',
    tree: 'packages',
    why: 'deprecated markers are searched in shipped package source; the absence of that source is not their absence',
  },
  {
    file: 'scan-evals-neutrality.mjs',
    finder: 'findEvalsNeutralityFindings',
    tree: 'packages',
    why: 'a library-neutrality floor that found no library has not found it neutral — the same repair, and the same reasoning, as scan-memory-neutrality',
  },
  {
    file: 'scan-memory-mirror.mjs',
    finder: 'collectMemoryMirrorFindings',
    tree: '.agents/memory',
    why: 'memory-mirroring.md makes the in-repo memory corpus mandatory HERE, so its absence is a broken checkout rather than a repository that has not started one',
  },
  {
    file: 'scan-orchestration-map.mjs',
    finder: 'collectOrchestrationMapFindings',
    tree: '.claude/agents',
    why: 'the agent definitions are the set the map is checked against; with none, "every agent is listed" is true of nothing (the missing MAP was already an error — the missing SUBJECT was not)',
  },
  {
    // INFRA-078. MEASURED on a bare root, not assumed: throws `.claude/hooks, .claude/settings.json
    // missing`. Both sides are named because either alone is unjudgeable — a hooks directory with
    // no settings file has no registrations to check, and settings with no hooks directory has no
    // files to check them against.
    file: 'scan-hook-registration.mjs',
    finder: 'collectHookRegistrationFindings',
    tree: '.claude/hooks and .claude/settings.json',
    why: 'it compares the hook files against the matchers that call them; over a root with neither, "every hook is registered" is a claim about no hooks — which is the exact green this scan was written to end one level down',
  },
  {
    file: 'scan-spec-research.mjs',
    finder: 'collectSpecResearchFindings',
    tree: '.agents/spec-docs',
    why: 'the spec-doc pipeline is the corpus and every stage directory was optional, so a root with none of them printed a pass over nothing',
  },
];

/**
 * Registered finders NOT behaviourally pinned by this scan, each carrying the verdict actually
 * MEASURED when it was handed a root without its governed tree (2026-07-26, this tree).
 *
 * This is a ledger, not an allowlist — and it is deliberately not uniform, because the measurements
 * were not. `vacuous` entries are live instances of the audited defect, recorded unfixed in
 * HARNESS-052. `fail-closed` entries already behave correctly but are not pinned here: pinning them
 * needs their governed tree named accurately, and some (e.g. `scan-unearned-done-claims`) fail
 * closed only INCIDENTALLY — via a stale-allowlist assertion rather than a deliberate check — so
 * pinning them as-is would certify a property they do not actually hold. Entries leave this list by
 * being fixed or accurately pinned, never by being deleted.
 *
 * Every `measured` value here was produced by executing the finder, not by reading it.
 */
export const PENDING_CLASSIFICATION = [

  // INFRA-061: the ci.yml audit's two scans landed (#1474) after this guard did (#1480) and were
  // never classified, so `harness:test` was red on develop itself. Measured here rather than
  // assumed, twice each, by executing the finder against a bare root:
  //   findRequiredCheckNeedsFindings  → fail-closed. PROMOTED to MANDATORY_TREE_GUARDS by D9
  //     (#1481), which left this duplicate entry behind — a finder in both tables fails this
  //     scan's exactly-one rule, so `develop` was red on it. Removed here (INFRA-062); the
  //     MANDATORY entry is the stronger of the two, since it re-proves the behaviour by execution.
  //   findTestSelectionFindings       → VACUOUS — a live instance of the audited defect. A
  //     test-selection-tolerance floor handed a root with no CI workflow reports nothing to fix,
  //     which is the same answer it gives for a correct one. Recorded unfixed and owned by
  //     INFRA-060, not silently pinned as though it were sound.
  {
    // Measured 2026-07-26: the FINDER returns `{findings: [], invocations: 0}` on an empty root —
    // vacuous by itself. Its `main()` treats `invocations === 0` as a failure, so the CLI is
    // fail-closed while the exported finder is not. Recorded rather than pinned because
    // MANDATORY_TREE_GUARDS asserts the finder's behaviour, and pinning it there would certify a
    // property the finder does not hold.
    file: 'scan-test-selection-tolerance.mjs',
    finder: 'findTestSelectionFindings',
    measured: 'vacuous',
  },
  {
    // Measured 2026-07-26: returns `[]` on a bare root. It is a pure ENUMERATOR — it lists the
    // vitest configs it finds and renders no verdict — so emptiness is its honest answer, not a
    // silent pass. The verdict is `findCeilingFindings`, pinned in MANDATORY_TREE_GUARDS, and its
    // `main()` turns `inspected === 0` into exit 1. Recorded as vacuous rather than pinned, because
    // pinning it would certify a fail-closed property this function does not have and does not
    // need.
    file: 'scan-vitest-resource-ceiling.mjs',
    finder: 'findVitestConfigs',
    measured: 'vacuous',
  },
  {
    file: 'check-agent-server-boundary.mjs',
    finder: 'findAgentServerBoundaryFindings',
    measured: 'fail-closed',
  },
  {
    file: 'check-background-workspace-conformance.mjs',
    finder: 'findBackgroundWorkspaceConformanceFindings',
    measured: 'fail-closed',
  },
  {
    // RE-MEASURED 2026-07-27 in HARNESS-052's second pass and DELIBERATELY left here. It returns the
    // exemptions its sibling APPLIED, so an empty result is the honest answer for a tree with no
    // exemptions in it — the verdict is `findBackgroundWorkspaceConformanceFindings`, which measures
    // fail-closed. Forcing this one to throw would certify a property it does not hold and does not
    // need, which is the mistake of treating this ledger as a to-do list.
    file: 'check-background-workspace-conformance.mjs',
    finder: 'findUsedExemptions',
    measured: 'vacuous',
  },
  {
    file: 'check-build-output-contracts.mjs',
    finder: 'findBuildOutputContractFindings',
    measured: 'fail-closed',
  },
  {
    file: 'check-capability-placement.mjs',
    finder: 'findCapabilityPlacementFindings',
    measured: 'fail-closed',
  },
  {
    file: 'check-functional-coverage.mjs',
    finder: 'collectFunctionalCoverageFindings',
    measured: 'fail-closed',
  },
  {
    file: 'scan-conflict-markers.mjs',
    finder: 'findConflictMarkerFindings',
    measured: 'fail-closed',
  },
  {
    // RE-MEASURED 2026-07-26 after HARNESS-053 replaced this scan's presence-only body with a real
    // freshness comparison: still `fail-closed`, but INCIDENTALLY, so it stays here rather than
    // being promoted. `finder(bare)` supplies one argument, `scopes` is undefined, and
    // `scopes.filter` throws — a missing-argument crash, not a check on a governed tree. This
    // finder has no tree of its own to be absent: its subject is the package set the CALLER
    // enumerates. Pinning it in MANDATORY_TREE_GUARDS would certify a property it does not hold.
    //
    // The gap that reasoning leaves was measured separately rather than left implied, and it was
    // real: the CLI's `main()` — which DOES enumerate, via `listWorkspaceScopes()` — printed
    // "dist/ present on all 0 package(s)" and exited 0 for a `pnpm-workspace.yaml` resolving to
    // zero packages. That pass-over-nothing is this scan's own subject, and it is now an exit 1
    // pinned by a test in `__tests__/scan-dist-freshness.test.mjs`. A root with no manifest at all
    // already threw out of `listWorkspaceScopes`.
    file: 'scan-dist-freshness.mjs',
    finder: 'collectDistFreshnessResults',
    measured: 'fail-closed',
  },
  {
    file: 'scan-guard-scope-fail-closed.mjs',
    finder: 'findGuardScopeFindings',
    measured: 'fail-closed',
    selfReferential: true,
  },
  { file: 'scan-hook-catalog.mjs', finder: 'collectFiringEvents', measured: 'fail-closed' },
  { file: 'scan-hook-catalog.mjs', finder: 'findHookCatalogFindings', measured: 'fail-closed' },
  {
    // Left vacuous DELIBERATELY (re-measured 2026-07-27). Its governed tree is `node_modules`, which
    // is not checked in: a fresh clone before `pnpm install`, and every git worktree in this repo's
    // own parallel-agent workflow, legitimately has none. Making its absence an error would fail on
    // a correct tree, and a guard that fires on correct data is one that gets suppressed. The
    // finder returns `undefined` rather than `[]` for exactly this reason, so its caller can tell
    // "no copies" from "nothing to look in".
    file: 'scan-legacy-typescript.mjs',
    finder: 'collectInstalledCopies',
    measured: 'vacuous',
  },
  {
    file: 'scan-legacy-typescript.mjs',
    finder: 'findLegacyTypeScriptFindings',
    measured: 'fail-closed',
  },
  {
    file: 'scan-main-required-checks.mjs',
    finder: 'findRequiredCheckFindings',
    measured: 'fail-closed',
  },
  {
    file: 'scan-review-findings.mjs',
    finder: 'collectReviewFindingsFindings',
    measured: 'fail-closed',
  },
  {
    file: 'scan-unearned-done-claims.mjs',
    finder: 'findUnearnedDoneClaimFindings',
    measured: 'fail-closed',
  },
];

/** Ledger entries measured to report a pass over an absent governed tree. */
export const measuredVacuous = () =>
  PENDING_CLASSIFICATION.filter((entry) => entry.measured === 'vacuous');

/** Scan scripts registered in `run-all-scans.mjs`, as bare filenames. Parsed, never hand-listed. */
export function registeredScanFiles(root = WORKSPACE_ROOT) {
  // Comments are stripped first (HARNESS-052): the raw text of the registration file names scans in
  // its own docstrings and in `// …` notes beside table entries, so a scan that had been COMMENTED
  // OUT of the table — or deleted from it and merely mentioned — still counted as registered. That
  // is the presence-of-a-string shape this item's second axis is about, in this scan's own
  // derivation. Structure lives in the array; comments are prose.
  const source = stripJsComments(readFileSync(path.join(root, REGISTRATION_FILE), 'utf8'));
  const files = [...source.matchAll(/scripts\/harness\/([a-z0-9-]+\.mjs)/g)].map((m) => m[1]);
  if (files.length === 0)
    throw new Error(
      `${REGISTRATION_FILE} parsed to zero registered scans. An empty registration list would ` +
        "satisfy every assertion below vacuously — which is this scan's own defect.",
    );
  return [...new Set(files)].sort();
}

/**
 * Exported finders of one harness script that take a `root`. The shape that makes a guard checkable
 * here.
 *
 * THIS REGEX IS THE WHOLE RULE, so its narrowness is the whole hole. It first read
 * `export function (find…)\(\s*root\s*=`, which matched neither `export async function` nor a
 * `collect…` finder nor a `root` parameter without a default. Measured: 20 of 41 finders derived,
 * and this scan did not classify ITSELF — `findGuardScopeFindings` is `async`. Falsified by
 * registering a scan exporting `export async function findBogusFindings(root = X) { return []; }`,
 * unconditionally vacuous for every root: the completeness rule passed. Changing the single keyword
 * `async function` → `function` made the same file fail. A rule whose verdict turns on a keyword is
 * measuring spelling, not structure — which is this scan's own subject, one level up.
 */
export function rootFinderExports(sourceText) {
  return [
    ...stripJsComments(String(sourceText ?? '')).matchAll(
      /export\s+(?:async\s+)?function\s+((?:find|collect)[A-Za-z0-9_]*)\(\s*root\b/g,
    ),
  ]
    .map((match) => match[1])
    .sort();
}

/**
 * Remove block and line comments before matching declarations.
 *
 * Not cosmetic. Without it this scan matched the `export async function findBogusFindings(root = X)`
 * written in its OWN docstring above and derived a finder for a function that does not exist — a
 * scan reading its own documentation as evidence, which is the failure mode `scan-orchestration-map`
 * and `check-test-coverage-scripts` were separately found to have. A declaration is code, so only
 * code is searched.
 */
export function stripJsComments(sourceText) {
  return String(sourceText ?? '')
    .replace(/\/\*[\s\S]*?\*\//g, '')
    .replace(/(^|[^:])\/\/[^\n]*/g, '$1');
}

/** Every `{ file, finder }` pair this scan is responsible for classifying, derived from the tree. */
export function derivedFinders(root = WORKSPACE_ROOT) {
  const pairs = [];
  for (const file of registeredScanFiles(root)) {
    const source = readFileSync(path.join(root, HARNESS_DIR, file), 'utf8');
    for (const finder of rootFinderExports(source)) pairs.push({ file, finder });
  }
  return pairs;
}

const keyOf = (entry) => `${entry.file}#${entry.finder}`;

/** Rule 1: every derived finder classified exactly once, and no table entry left behind. */
export function classificationFindings(root = WORKSPACE_ROOT) {
  const findings = [];
  const derived = new Set(derivedFinders(root).map(keyOf));
  const mandatory = MANDATORY_TREE_GUARDS.map(keyOf);
  const pending = PENDING_CLASSIFICATION.map(keyOf);
  const declared = new Set([...mandatory, ...pending]);

  for (const key of [...derived].sort()) {
    if (!declared.has(key))
      findings.push({
        subject: key,
        detail:
          'is a registered scan exporting a root finder, and is in neither MANDATORY_TREE_GUARDS ' +
          'nor PENDING_CLASSIFICATION. Classify it: run it against a root without its governed ' +
          'tree and record whether it fails closed.',
      });
  }
  for (const key of [...declared].sort()) {
    if (!derived.has(key))
      findings.push({
        subject: key,
        detail:
          'is declared here but is no longer a registered scan exporting that finder. A stale ' +
          'declaration is a rule that guards nothing — remove it or fix the name.',
      });
  }
  const both = mandatory.filter((key) => pending.includes(key));
  for (const key of both)
    findings.push({
      subject: key,
      detail: 'appears in BOTH tables — it must appear in exactly one.',
    });

  // EXACTLY ONE means exactly one, including within a single table. The cross-table rule above was
  // the whole of it, so `scan-test-selection-tolerance#findTestSelectionFindings` sat in
  // PENDING_CLASSIFICATION TWICE — measured twice, counted twice in the summary line, and a future
  // repair would have had to be noticed twice. A rule stated as "exactly one" that only checks
  // "not two tables" is the audited shape in miniature.
  for (const [table, keys] of [
    ['MANDATORY_TREE_GUARDS', mandatory],
    ['PENDING_CLASSIFICATION', pending],
  ]) {
    const counts = new Map();
    for (const key of keys) counts.set(key, (counts.get(key) ?? 0) + 1);
    for (const [key, count] of counts) {
      if (count > 1)
        findings.push({
          subject: key,
          detail: `appears ${count} times in ${table} — it must appear exactly once.`,
        });
    }
  }
  return findings;
}

/** Rule 2: run one mandatory guard against a root lacking its governed tree. */
async function behaviourFinding(entry, root) {
  const module = await import(pathToFileURL(path.join(root, HARNESS_DIR, entry.file)).href);
  const finder = module[entry.finder];
  if (typeof finder !== 'function')
    return {
      subject: keyOf(entry),
      detail: `does not export \`${entry.finder}\` as a function — the declaration is stale.`,
    };

  const bare = mkdtempSync(path.join(tmpdir(), 'guard-scope-'));
  try {
    let result;
    try {
      // AWAITED (HARNESS-052): without this an async finder resolves to a Promise, which is neither
      // an array nor `{findings}`, so it would be reported as a violation no matter how it behaves.
      // That defect and the sync-only derivation regex above masked each other exactly.
      result = await finder(bare);
    } catch {
      return undefined; // threw on an unreadable root — fail-closed, which is the requirement
    }
    const list = Array.isArray(result) ? result : (result?.findings ?? null);
    const otherSignals =
      result && !Array.isArray(result)
        ? Object.entries(result).some(
            ([key, value]) => key !== 'findings' && Array.isArray(value) && value.length > 0,
          )
        : false;
    if ((list?.length ?? 0) > 0 || otherSignals) return undefined; // reported something — fail-closed
    return {
      subject: keyOf(entry),
      detail:
        `returned an EMPTY finding list for a root with no \`${entry.tree}\`. ${entry.why}. ` +
        'Make the missing tree an error, not a pass.',
    };
  } finally {
    rmSync(bare, { recursive: true, force: true });
  }
}

/**
 * How long one ledger measurement may take before it is treated as unmeasurable.
 *
 * Importing an arbitrary harness module executes its top-level code, and some of the 44 modules do
 * real work there. One left a promise that never settled, which killed the whole scan with node's
 * `ERR_UNSETTLED_TOPLEVEL_AWAIT` (exit 13) and NO output — a guard dying silently mid-run while
 * measuring other guards for dying silently. Bounded and caught, an unmeasurable entry becomes a
 * finding instead.
 */
const MEASURE_TIMEOUT_MS = 15_000;

/**
 * The verdict a finder actually produces when handed a root without its governed tree.
 *
 * EXPORTED so the ledger is regenerated by the exact code path that later verifies it. The first
 * ledger was generated by a throwaway `node -e` loop whose catch scored ANY failure as
 * `fail-closed` — and several harness modules call `pathToFileURL(process.argv[1])` at import time,
 * which throws under `node -e` where `argv[1]` is undefined. Those entries recorded an import crash
 * as correct behaviour. Two measurement paths that disagree is one measurement path too many.
 */
export async function measureFinder(entry, root = WORKSPACE_ROOT) {
  const bare = mkdtempSync(path.join(tmpdir(), 'guard-scope-measure-'));
  let timer;
  try {
    const measure = (async () => {
      const module = await import(pathToFileURL(path.join(root, HARNESS_DIR, entry.file)).href);
      const finder = module[entry.finder];
      if (typeof finder !== 'function') return 'missing';
      let result;
      try {
        result = await finder(bare);
      } catch {
        return 'fail-closed';
      }
      const list = Array.isArray(result) ? result : (result?.findings ?? null);
      const otherSignals =
        result && !Array.isArray(result)
          ? Object.entries(result).some(
              ([key, value]) => key !== 'findings' && Array.isArray(value) && value.length > 0,
            )
          : false;
      return (list?.length ?? 0) > 0 || otherSignals ? 'fail-closed' : 'vacuous';
    })();
    const deadline = new Promise((resolve) => {
      timer = setTimeout(() => resolve('unmeasurable'), MEASURE_TIMEOUT_MS);
    });
    // An import that throws is `unmeasurable`, NOT `fail-closed`: a module that cannot even be
    // loaded has told us nothing about how its finder behaves, and scoring that as correct would be
    // this scan certifying a guard it never ran.
    return await Promise.race([measure, deadline]).catch(() => 'unmeasurable');
  } finally {
    clearTimeout(timer);
    rmSync(bare, { recursive: true, force: true });
  }
}

/**
 * Rule 3: every ledger entry's recorded `measured` value must still be the value the finder
 * produces.
 *
 * Added after this scan's ledger went stale within an hour of being written — repairing
 * `scan-conflict-markers` turned it fail-closed while the ledger still recorded it `vacuous`. A
 * debt ledger nobody re-measures becomes a set of claims about the past presented as facts about the
 * present, which is the audited defect in documentation form. The recorded verdicts are now
 * RE-EXECUTED on every run rather than trusted, so an entry cannot silently drift in either
 * direction: a scan that got fixed must be moved out, and one that regressed must be caught.
 */
export async function ledgerDriftFindings(root = WORKSPACE_ROOT) {
  const findings = [];
  for (const entry of PENDING_CLASSIFICATION) {
    // This scan's own finder cannot be measured this way: measuring it runs this loop, which
    // measures it again. It stays CLASSIFIED (rule 1 still requires that, and it is what caught the
    // async blind spot) but is exempt from rule 3, with the reason stated rather than skipped.
    if (entry.selfReferential) continue;
    const actual = await measureFinder(entry, root);
    if (actual === entry.measured) continue;
    findings.push({
      subject: keyOf(entry),
      detail:
        actual === 'missing'
          ? `is recorded here but no longer exports \`${entry.finder}\`.`
          : actual === 'unmeasurable'
            ? 'could not be measured — importing it threw, or it did not settle within ' +
              `${MEASURE_TIMEOUT_MS}ms. That is not a pass: nothing was learned about how it behaves.`
            : `is recorded as \`${entry.measured}\` but now measures \`${actual}\`. ` +
              (actual === 'fail-closed'
                ? 'It was repaired — move it to MANDATORY_TREE_GUARDS with its governed tree named, ' +
                  'so the fix is pinned rather than merely noted.'
                : 'It regressed — a guard that used to fail closed now reports a pass over a tree ' +
                  'it never read.'),
    });
  }
  return findings;
}

/** Findings across all three rules. */
export async function findGuardScopeFindings(root = WORKSPACE_ROOT) {
  const findings = classificationFindings(root);
  for (const entry of MANDATORY_TREE_GUARDS) {
    const finding = await behaviourFinding(entry, root);
    if (finding) findings.push(finding);
  }
  findings.push(...(await ledgerDriftFindings(root)));
  return findings;
}

export async function main() {
  const findings = await findGuardScopeFindings();
  if (findings.length > 0) {
    process.stdout.write('guard-scope-fail-closed scan failed (HARNESS-052):\n');
    for (const finding of findings) {
      process.stdout.write(`  - ${finding.subject}: ${finding.detail}\n`);
    }
    process.stdout.write(
      'A guard that reports a pass over a tree it never read is the defect it was built to catch,\n' +
        'one level up. See HARNESS-052.\n',
    );
    process.exitCode = 1;
    return;
  }
  process.stdout.write(
    `guard-scope-fail-closed scan passed (${MANDATORY_TREE_GUARDS.length} guard(s) proven ` +
      `fail-closed by execution; ${measuredVacuous().length} measured VACUOUS and recorded unfixed ` +
      `in HARNESS-052, ${PENDING_CLASSIFICATION.length - measuredVacuous().length} fail closed but ` +
      'are not pinned here). This is not a claim that no guard can be satisfied vacuously.\n',
  );
}

const isDirectExecution =
  process.argv[1] !== undefined &&
  path.resolve(process.argv[1]) === path.resolve(import.meta.filename);
if (isDirectExecution) {
  await main();
}
