{
  "item": "STRUCT-012",
  "reason": "Same-family sibling edges that existed when the FAMILY-SIBLINGS rule was armed (agent-<family>-<child> depending on agent-<family>-<other>). Each is a lower layer wearing a sibling name; the rule forbids new ones from the day it is armed and this set may only SHRINK -- an entry whose edge no longer exists is itself a finding, so the freeze cannot outlive the debt. STRUCT-012 S4 removed the final transport edge by moving the presentation packages into the agent-ui family; the provider edge has its own root item.",
  "frozen": {
    "@robota-sdk/agent-provider-openai -> @robota-sdk/agent-provider-openai-compatible": "agent-provider-openai-compatible is a lower layer named as a sibling; separate root item (STRUCT-012 § Out of scope)"
  }
}
