{
  "item": "HARNESS-117",
  "reason": "Rule identifiers a harness scan EMITS for which no normative document states a rule. Each entry is enforcement a contributor cannot read, cite, argue with, or amend deliberately -- it can only be tripped over. Frozen at adoption so the debt is counted on every run rather than hidden; the set may only SHRINK. Writing the missing statements is a documentation migration filed separately, not this scan's job. Emptied by issue #2188: every identifier is now stated in .agents/project-structure.md.",
  "frozen": {}
}
