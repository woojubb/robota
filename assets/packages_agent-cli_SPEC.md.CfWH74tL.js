import{_ as t,c as s,o as a,ag as n}from"./chunks/framework.Bw-5EFTY.js";const u=JSON.parse('{"title":"SPEC.md — @robota-sdk/agent-cli","description":"","frontmatter":{},"headers":[],"relativePath":"packages/agent-cli/SPEC.md","filePath":"packages/agent-cli/SPEC.md"}'),o={name:"packages/agent-cli/SPEC.md"};function i(d,e,l,r,c,p){return a(),s("div",null,e[0]||(e[0]=[n(`<h1 id="spec-md-—-robota-sdk-agent-cli" tabindex="-1">SPEC.md — @robota-sdk/agent-cli <a class="header-anchor" href="#spec-md-—-robota-sdk-agent-cli" aria-label="Permalink to &quot;SPEC.md — @robota-sdk/agent-cli&quot;">​</a></h1><h2 id="scope" tabindex="-1">Scope <a class="header-anchor" href="#scope" aria-label="Permalink to &quot;Scope&quot;">​</a></h2><p>Interactive terminal AI coding assistant. A React + Ink-based TUI, corresponding to Claude Code. A <strong>thin CLI layer</strong> built on top of agent-sdk, responsible only for the terminal UI.</p><h2 id="boundaries" tabindex="-1">Boundaries <a class="header-anchor" href="#boundaries" aria-label="Permalink to &quot;Boundaries&quot;">​</a></h2><ul><li>Does NOT own Session/SessionStore — handled internally by <code>@robota-sdk/agent-sdk</code>; CLI must NOT import from <code>@robota-sdk/agent-sessions</code></li><li>Does NOT own tools — assembled internally by <code>@robota-sdk/agent-sdk</code>; CLI must NOT import from <code>@robota-sdk/agent-tools</code></li><li>Does NOT own permissions/hooks — public types imported from <code>@robota-sdk/agent-core</code>; permission callback type (<code>TInteractivePermissionHandler</code>) owned by <code>@robota-sdk/agent-sdk</code></li><li>Does NOT own config/context loading — loaded internally by <code>InteractiveSession</code> constructor</li><li>OWNS: AI provider creation (reads config, selects provider package, creates instance, passes to <code>InteractiveSession</code>)</li><li>Does NOT own <code>InteractiveSession</code> — imported from <code>@robota-sdk/agent-sdk</code></li><li>Does NOT own <code>CommandRegistry</code>, <code>BuiltinCommandSource</code>, <code>SkillCommandSource</code> — all imported from <code>@robota-sdk/agent-sdk</code></li><li>Does NOT use <code>SystemCommandExecutor</code> directly — uses <code>session.executeCommand(name, args)</code> instead</li><li>Does NOT own ITerminalOutput/ISpinner — SSOT is <code>@robota-sdk/agent-core</code></li><li>OWNS: Ink TUI components, permission-prompt (terminal UI), CLI argument parsing, <code>useInteractiveSession</code> hook</li><li>Does NOT own <code>PluginCommandSource</code> — imported from <code>@robota-sdk/agent-sdk</code></li><li>Does NOT own <code>plugin-hooks-merger</code> — moved to <code>@robota-sdk/agent-sdk</code></li></ul><h2 id="import-rules" tabindex="-1">Import Rules <a class="header-anchor" href="#import-rules" aria-label="Permalink to &quot;Import Rules&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Source</th><th>Allowed</th><th>Examples</th></tr></thead><tbody><tr><td><code>agent-sdk</code></td><td>SDK-owned APIs</td><td><code>InteractiveSession</code>, <code>TInteractivePermissionHandler</code></td></tr><tr><td><code>agent-core</code></td><td>Public types + utilities only</td><td><code>TUniversalMessage</code>, <code>TPermissionMode</code>, <code>createSystemMessage</code>, <code>getModelName</code></td></tr><tr><td><code>agent-core</code></td><td>❌ Internal engine</td><td><s><code>Robota</code></s>, <s><code>ExecutionService</code></s>, <s><code>ConversationStore</code></s></td></tr><tr><td><code>agent-sessions</code></td><td>❌ Forbidden</td><td>SDK provides its own session and permission types</td></tr><tr><td><code>agent-tools</code></td><td>❌ Forbidden</td><td>SDK assembles tools internally</td></tr><tr><td><code>agent-provider-*</code></td><td>✅ Provider creation only</td><td><code>AnthropicProvider</code> (CLI picks which to use; currently only anthropic supported)</td></tr></tbody></table><h2 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h2><p>The CLI is a pure TUI layer. All business logic (session lifecycle, slash command execution, tool orchestration, abort handling) lives in <code>@robota-sdk/agent-sdk</code>&#39;s <code>InteractiveSession</code>. The CLI:</p><ol><li>Reads config to determine which provider to use.</li><li>Creates the provider instance (e.g., <code>new AnthropicProvider(options)</code>).</li><li>Creates <code>InteractiveSession({ cwd, provider })</code> — config and context loading happen internally inside the SDK.</li><li>Subscribes to <code>InteractiveSession</code> events and converts them to React state for rendering.</li></ol><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>bin.ts → cli.ts (arg parsing + provider creation)</span></span>
<span class="line"><span>              └── ui/render.tsx → App.tsx (Ink TUI)</span></span>
<span class="line"><span>                    ├── useInteractiveSession (ONLY React↔SDK bridge)</span></span>
<span class="line"><span>                    │   ├── InteractiveSession({ cwd, provider })</span></span>
<span class="line"><span>                    │   │   (from @robota-sdk/agent-sdk; config/context loaded internally)</span></span>
<span class="line"><span>                    │   ├── TuiStateManager    (owned by agent-cli)</span></span>
<span class="line"><span>                    │   │   holds history: IHistoryEntry[]  ← primary state for message list</span></span>
<span class="line"><span>                    │   │   syncs from interactiveSession.getFullHistory() on each update</span></span>
<span class="line"><span>                    │   ├── CommandRegistry    (from @robota-sdk/agent-sdk)</span></span>
<span class="line"><span>                    │   │   ├── BuiltinCommandSource  (from @robota-sdk/agent-sdk)</span></span>
<span class="line"><span>                    │   │   ├── SkillCommandSource    (from @robota-sdk/agent-sdk)</span></span>
<span class="line"><span>                    │   │   └── PluginCommandSource   (from @robota-sdk/agent-sdk)</span></span>
<span class="line"><span>                    │   └── session.executeCommand()  (slash commands routed via SDK)</span></span>
<span class="line"><span>                    ├── MessageList.tsx        (renders IHistoryEntry[]; EntryItem dispatches on category)</span></span>
<span class="line"><span>                    ├── InputArea.tsx          (bottom input area, slash detection)</span></span>
<span class="line"><span>                    ├── StatusBar.tsx          (status bar, shows &quot;Thinking...&quot; during run())</span></span>
<span class="line"><span>                    ├── PermissionPrompt.tsx   (arrow-key selection)</span></span>
<span class="line"><span>                    └── SlashAutocomplete.tsx  (command popup with scroll)</span></span></code></pre></div><p>Dependency chain:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>agent-cli ─→ agent-sdk ─→ agent-sessions ─→ agent-core</span></span>
<span class="line"><span>  │            ├─→ agent-tools ────────────→ agent-core</span></span>
<span class="line"><span>  │            └─────────────────────────→ agent-core  (direct: types, utilities)</span></span>
<span class="line"><span>  ├──────────────────────────────────────→ agent-core  (direct: public types only)</span></span>
<span class="line"><span>  └──────────────────────────────────────→ agent-provider-* (provider creation)</span></span></code></pre></div><h2 id="statusbar-display" tabindex="-1">StatusBar Display <a class="header-anchor" href="#statusbar-display" aria-label="Permalink to &quot;StatusBar Display&quot;">​</a></h2><p>The StatusBar shows real-time session information:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌──────────────────────────────────────────────────────────┐</span></span>
<span class="line"><span>│ Mode: default  |  Claude Sonnet 4.6  |  Context: 45% (90K/200K)  |  msgs: 12 │</span></span>
<span class="line"><span>└──────────────────────────────────────────────────────────┘</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Field</th><th>Source</th><th>Description</th></tr></thead><tbody><tr><td>Mode</td><td><code>session.getPermissionMode()</code></td><td>Current permission mode</td></tr><tr><td>Model</td><td><code>getModelName(config.provider.model)</code></td><td>Human-readable model name (e.g., &quot;Claude Sonnet 4.6&quot;)</td></tr><tr><td>Context</td><td><code>session.getContextState().usedPercentage</code></td><td>Context usage with K/M formatting (e.g., &quot;90K/1M&quot;)</td></tr><tr><td>msgs</td><td>message count</td><td>Number of messages in conversation</td></tr><tr><td>Thinking</td><td>isThinking state</td><td>Shown during <code>session.run()</code> execution</td></tr></tbody></table><h3 id="context-color-coding" tabindex="-1">Context Color Coding <a class="header-anchor" href="#context-color-coding" aria-label="Permalink to &quot;Context Color Coding&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Range</th><th>Color</th><th>Meaning</th></tr></thead><tbody><tr><td>0-69%</td><td>Green</td><td>Healthy</td></tr><tr><td>70-89%</td><td>Yellow</td><td>Approaching limit</td></tr><tr><td>90%+</td><td>Red</td><td>Near limit, compaction imminent</td></tr></tbody></table><h2 id="context-management-cli-layer" tabindex="-1">Context Management (CLI Layer) <a class="header-anchor" href="#context-management-cli-layer" aria-label="Permalink to &quot;Context Management (CLI Layer)&quot;">​</a></h2><h3 id="compact-slash-command" tabindex="-1"><code>/compact</code> Slash Command <a class="header-anchor" href="#compact-slash-command" aria-label="Permalink to &quot;\`/compact\` Slash Command&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/compact                          # Default compaction</span></span>
<span class="line"><span>/compact focus on API changes     # Custom focus instructions</span></span></code></pre></div><ul><li>Calls <code>session.compact(instructions)</code></li><li>Displays before/after context percentage</li><li>Shows &quot;Context compressed: 85% → 32%&quot; message</li></ul><h3 id="auto-compaction-notification" tabindex="-1">Auto-Compaction Notification <a class="header-anchor" href="#auto-compaction-notification" aria-label="Permalink to &quot;Auto-Compaction Notification&quot;">​</a></h3><p>When auto-compaction triggers (at ~83.5% threshold), the UI shows a system message notifying the user.</p><h2 id="tool-call-display" tabindex="-1">Tool Call Display <a class="header-anchor" href="#tool-call-display" aria-label="Permalink to &quot;Tool Call Display&quot;">​</a></h2><h3 id="real-time-tool-execution-streaming" tabindex="-1">Real-Time Tool Execution (Streaming) <a class="header-anchor" href="#real-time-tool-execution-streaming" aria-label="Permalink to &quot;Real-Time Tool Execution (Streaming)&quot;">​</a></h3><p>During <code>session.run()</code>, tool execution is displayed in real-time via the <code>onToolExecution</code> callback. The streaming display shows <strong>Tools: first, then Robota:</strong> in execution order:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Tools:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  ✓ Read(/src/index.ts)</span></span>
<span class="line"><span>  ✓ Bash(ls -la)</span></span>
<span class="line"><span>  ⟳ Glob(**/*.md)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Robota:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  Checking the file structure now...</span></span></code></pre></div><p><strong>Behavior:</strong></p><ul><li><code>onToolExecution</code> fires <code>start</code> when a tool begins and <code>end</code> when it completes</li><li>Running tools show <code>⟳</code> (yellow), completed tools show <code>✓</code> (green)</li><li>Format: <code>ToolName(firstArgValue)</code> — first argument truncated to 80 chars, matching post-run summary style</li><li>Completed tools remain visible until <code>session.run()</code> finishes (not removed on <code>end</code>)</li><li><code>Tools:</code> and <code>Robota:</code> sections each have a blank line below the label and between sections</li><li>When no tools and no streaming text, renders nothing (empty fragment); &quot;Thinking...&quot; is shown by <code>StatusBar</code></li></ul><h3 id="post-run-tool-summary" tabindex="-1">Post-Run Tool Summary <a class="header-anchor" href="#post-run-tool-summary" aria-label="Permalink to &quot;Post-Run Tool Summary&quot;">​</a></h3><p>After each <code>session.run()</code> completes, tool calls from the session history are extracted and displayed as a single grouped message:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Tool: [5 tools]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  Read(/Users/jungyoun/Documents/dev/robota/.agents/tasks/apps-web-sep...)</span></span>
<span class="line"><span>  Bash(ls -la .agents/tasks/)</span></span>
<span class="line"><span>  Glob(**/*.md)</span></span></code></pre></div><ul><li>All tool calls from a run are grouped into one <code>role: &#39;tool&#39;</code> message</li><li>Format: <code>ToolName(firstArgValue)</code> — first argument value extracted from JSON, truncated to 80 chars</li><li>Displayed after the assistant response in the message list</li></ul><h2 id="slash-commands" tabindex="-1">Slash Commands <a class="header-anchor" href="#slash-commands" aria-label="Permalink to &quot;Slash Commands&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Command</th><th>Description</th></tr></thead><tbody><tr><td><code>/help</code></td><td>Show available commands</td></tr><tr><td><code>/clear</code></td><td>Clear conversation history</td></tr><tr><td><code>/mode [mode]</code></td><td>Show/change permission mode</td></tr><tr><td><code>/model [model]</code></td><td>Select AI model (shows confirmation prompt, restarts session)</td></tr><tr><td><code>/language [lang]</code></td><td>Set response language (ko, en, ja, zh), saves and restarts</td></tr><tr><td><code>/compact [instructions]</code></td><td>Compress context window</td></tr><tr><td><code>/cost</code></td><td>Show session info</td></tr><tr><td><code>/context</code></td><td>Context window info</td></tr><tr><td><code>/permissions</code></td><td>Permission rules</td></tr><tr><td><code>/plugin [subcommand]</code></td><td>Plugin management</td></tr><tr><td><code>/exit</code></td><td>Exit CLI</td></tr></tbody></table><h3 id="slash-command-autocomplete" tabindex="-1">Slash Command Autocomplete <a class="header-anchor" href="#slash-command-autocomplete" aria-label="Permalink to &quot;Slash Command Autocomplete&quot;">​</a></h3><p>Typing <code>/</code> as the first character in the input triggers an autocomplete popup. The popup filters commands in real-time as the user types.</p><p><strong>Interaction:</strong></p><ul><li>Arrow Up/Down: Navigate items</li><li>Enter: Select highlighted item (inserts command, executes if no args needed)</li><li>Tab: Complete to common prefix</li><li>Esc: Dismiss popup, keep typed text</li><li>Backspace past <code>/</code>: Dismiss popup</li></ul><p><strong>Subcommand Navigation:</strong></p><p>Commands with subcommands (e.g., <code>/mode</code>, <code>/model</code>) show a nested submenu when selected:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&gt; /mode</span></span>
<span class="line"><span>+-------------------------------------+</span></span>
<span class="line"><span>|   plan                              |</span></span>
<span class="line"><span>|   default                           |</span></span>
<span class="line"><span>|   acceptEdits                       |</span></span>
<span class="line"><span>|   bypassPermissions                 |</span></span>
<span class="line"><span>+-------------------------------------+</span></span></code></pre></div><p><strong>Visual Grouping:</strong></p><p>Commands are grouped by source with separators: built-in commands appear first, followed by discovered skill commands.</p><h3 id="model-—-model-change-flow" tabindex="-1"><code>/model</code> — Model Change Flow <a class="header-anchor" href="#model-—-model-change-flow" aria-label="Permalink to &quot;\`/model\` — Model Change Flow&quot;">​</a></h3><p>The <code>/model</code> command lists available models as subcommands with the format <code>Claude Opus 4.6 (1M)</code>. Model definitions come from the <code>CLAUDE_MODELS</code> registry in <code>@robota-sdk/agent-core</code>.</p><p><strong>Subcommand display:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&gt; /model</span></span>
<span class="line"><span>+-------------------------------------+</span></span>
<span class="line"><span>|   Claude Opus 4.6 (1M)             |</span></span>
<span class="line"><span>|   Claude Sonnet 4.6 (1M)           |</span></span>
<span class="line"><span>|   Claude Haiku 4.5 (200K)          |</span></span>
<span class="line"><span>+-------------------------------------+</span></span></code></pre></div><p><strong>Model change flow:</strong></p><ol><li>User selects a model from the subcommand list</li><li>A <code>ConfirmPrompt</code> appears: &quot;Change model to Claude Opus 4.6? The CLI will restart.&quot;</li><li>If confirmed (Yes / <code>y</code>): settings are written to <code>~/.robota/settings.json</code> and the CLI exits (user restarts manually)</li><li>If cancelled (No / <code>n</code>): returns to normal input</li></ol><h3 id="confirmprompt-component" tabindex="-1">ConfirmPrompt Component <a class="header-anchor" href="#confirmprompt-component" aria-label="Permalink to &quot;ConfirmPrompt Component&quot;">​</a></h3><p>A reusable confirmation prompt with arrow-key selection (<code>ConfirmPrompt.tsx</code>). Used by <code>/model</code> change and available for other yes/no confirmations.</p><p><strong>Props:</strong></p><table tabindex="0"><thead><tr><th>Prop</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>message</code></td><td><code>string</code></td><td>—</td><td>Message above the options</td></tr><tr><td><code>options</code></td><td><code>string[]</code></td><td><code>[&#39;Yes&#39;, &#39;No&#39;]</code></td><td>Options to select from</td></tr><tr><td><code>onSelect</code></td><td><code>(index: number) =&gt; void</code></td><td>—</td><td>Callback with selected index</td></tr></tbody></table><p><strong>Interaction:</strong> Arrow keys to navigate, Enter to confirm. For 2-option prompts, <code>y</code> selects the first option, <code>n</code> selects the second.</p><h3 id="plugin-—-plugin-management" tabindex="-1"><code>/plugin</code> — Plugin Management <a class="header-anchor" href="#plugin-—-plugin-management" aria-label="Permalink to &quot;\`/plugin\` — Plugin Management&quot;">​</a></h3><p>The <code>/plugin</code> command manages bundle plugins. Subcommands:</p><table tabindex="0"><thead><tr><th>Subcommand</th><th>Description</th></tr></thead><tbody><tr><td><code>/plugin install &lt;name&gt;</code></td><td>Install a plugin from marketplace or local path</td></tr><tr><td><code>/plugin uninstall &lt;name&gt;</code></td><td>Remove an installed plugin</td></tr><tr><td><code>/plugin enable &lt;name&gt;</code></td><td>Enable a disabled plugin</td></tr><tr><td><code>/plugin disable &lt;name&gt;</code></td><td>Disable a plugin without uninstalling</td></tr><tr><td><code>/plugin list</code></td><td>List installed plugins with status</td></tr><tr><td><code>/plugin marketplace</code></td><td>Browse available plugins from configured sources</td></tr></tbody></table><p>Installed plugins contribute skills via <code>PluginCommandSource</code>, which discovers skills from each plugin&#39;s bundle manifest and makes them available as slash commands alongside project and user skills.</p><h2 id="react↔sdk-bridge" tabindex="-1">React↔SDK Bridge <a class="header-anchor" href="#react↔sdk-bridge" aria-label="Permalink to &quot;React↔SDK Bridge&quot;">​</a></h2><p><code>useInteractiveSession</code> is the single boundary between React and the SDK. It:</p><ol><li>Creates <code>InteractiveSession({ cwd, provider })</code> and <code>CommandRegistry</code> once (via <code>useRef</code> — never recreated on re-render). The provider instance is passed in from the caller; <code>InteractiveSession</code> handles config/context loading internally.</li><li>Creates a <code>TuiStateManager</code> instance that holds <code>history: IHistoryEntry[]</code> as the primary state for the message list. On each execution update (when <code>thinking</code> transitions to <code>false</code>, or on <code>complete</code>/<code>interrupted</code>), the hook delegates to <code>TuiStateManager</code> to sync state from <code>interactiveSession.getFullHistory()</code>.</li><li>Subscribes to <code>InteractiveSession</code> events (<code>text_delta</code>, <code>tool_start</code>, <code>tool_end</code>, <code>thinking</code>, <code>complete</code>, <code>interrupted</code>, <code>error</code>) and converts them to React state.</li><li>Exposes <code>handleSubmit</code>, <code>handleAbort</code>, <code>handleCancelQueue</code> as stable callbacks to the TUI.</li><li>Routes slash commands via <code>session.executeCommand(name, args)</code> — no <code>SystemCommandExecutor</code> is instantiated directly by the CLI.</li><li>Manages the permission queue (serialises concurrent permission requests).</li></ol><p>No other hook or component interacts with <code>InteractiveSession</code> directly.</p><h3 id="plugin-hook-merging" tabindex="-1">Plugin Hook Merging <a class="header-anchor" href="#plugin-hook-merging" aria-label="Permalink to &quot;Plugin Hook Merging&quot;">​</a></h3><p>Plugin hook merging (resolving <code>\${CLAUDE_PLUGIN_ROOT}</code> and merging hook groups) is handled internally by <code>@robota-sdk/agent-sdk</code>. The CLI does not perform hook merging.</p><h3 id="app-tsx" tabindex="-1">App.tsx <a class="header-anchor" href="#app-tsx" aria-label="Permalink to &quot;App.tsx&quot;">​</a></h3><p><code>App.tsx</code> is a thin JSX shell (~220 lines). It:</p><ul><li>Calls <code>useInteractiveSession</code> and <code>usePluginCallbacks</code>.</li><li>Wraps <code>handleSubmit</code> only to process TUI-specific side effects (<code>_pendingModelId</code>, <code>_pendingLanguage</code>, <code>_resetRequested</code>, <code>_exitRequested</code>, <code>_triggerPluginTUI</code>) that require Ink APIs (<code>useApp().exit</code>).</li><li>Contains no queue logic, no abort logic, no session business logic.</li></ul><h3 id="tool-list-visibility" tabindex="-1">Tool List Visibility <a class="header-anchor" href="#tool-list-visibility" aria-label="Permalink to &quot;Tool List Visibility&quot;">​</a></h3><p>The <code>StreamingIndicator</code> (showing active tools) is rendered when <code>isThinking || activeTools.length &gt; 0</code>. Streaming state (<code>streamBuf</code>, <code>activeTools</code>) is cleared at the <strong>start</strong> of a new execution (when <code>thinking: true</code>), not at the end. This means the tool list stays visible after execution completes or is aborted, until the next execution begins.</p><h2 id="command-registry-architecture" tabindex="-1">Command Registry Architecture <a class="header-anchor" href="#command-registry-architecture" aria-label="Permalink to &quot;Command Registry Architecture&quot;">​</a></h2><p>The slash command system uses an extensible registry pattern. Multiple <code>ICommandSource</code> implementations provide commands, and the <code>CommandRegistry</code> aggregates them. <code>CommandRegistry</code>, <code>BuiltinCommandSource</code>, and <code>SkillCommandSource</code> are all owned by <code>@robota-sdk/agent-sdk</code>. Slash command execution is routed through <code>session.executeCommand(name, args)</code> — the CLI does not instantiate <code>SystemCommandExecutor</code> directly. The CLI adds only <code>PluginCommandSource</code>.</p><h3 id="icommandsource-interface" tabindex="-1">ICommandSource Interface <a class="header-anchor" href="#icommandsource-interface" aria-label="Permalink to &quot;ICommandSource Interface&quot;">​</a></h3><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ICommandSource</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  getCommands</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ISlashCommand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="islashcommand-interface" tabindex="-1">ISlashCommand Interface <a class="header-anchor" href="#islashcommand-interface" aria-label="Permalink to &quot;ISlashCommand Interface&quot;">​</a></h3><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ISlashCommand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  name</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  description</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  source</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  skillContent</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Full SKILL.md content (skill commands only)</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  subcommands</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ISlashCommand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  execute</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">args</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> void</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Promise</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="command-sources" tabindex="-1">Command Sources <a class="header-anchor" href="#command-sources" aria-label="Permalink to &quot;Command Sources&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Source</th><th>Class</th><th>Owner</th><th>Description</th></tr></thead><tbody><tr><td>Built-in</td><td><code>BuiltinCommandSource</code></td><td><code>@robota-sdk/agent-sdk</code></td><td>Built-in commands with subcommands for /mode, /model</td></tr><tr><td>Skills</td><td><code>SkillCommandSource</code></td><td><code>@robota-sdk/agent-sdk</code></td><td>Discovered from 4 scan paths (see Skill Discovery)</td></tr><tr><td>Plugins</td><td><code>PluginCommandSource</code></td><td><code>@robota-sdk/agent-sdk</code></td><td>Skills provided by installed bundle plugins</td></tr></tbody></table><h3 id="skill-discovery-multi-path" tabindex="-1">Skill Discovery (Multi-Path) <a class="header-anchor" href="#skill-discovery-multi-path" aria-label="Permalink to &quot;Skill Discovery (Multi-Path)&quot;">​</a></h3><p>Skills are discovered at session start from directories scanned by <code>SkillCommandSource</code> (agent-sdk), in priority order (highest first, deduplicated by name). Paths are defined in agent-sdk&#39;s SPEC.md; the CLI uses them as-is:</p><table tabindex="0"><thead><tr><th>Priority</th><th>Path</th><th>Scope</th></tr></thead><tbody><tr><td>1</td><td><code>.claude/skills/*/SKILL.md</code></td><td>Project (Claude Code native)</td></tr><tr><td>2</td><td><code>.claude/commands/*.md</code></td><td>Project (Claude Code compatible)</td></tr><tr><td>3</td><td><code>~/.robota/skills/*/SKILL.md</code></td><td>User global (Robota native)</td></tr><tr><td>4</td><td><code>.agents/skills/*/SKILL.md</code></td><td>Project (Robota native)</td></tr></tbody></table><h3 id="skill-frontmatter-schema" tabindex="-1">Skill Frontmatter Schema <a class="header-anchor" href="#skill-frontmatter-schema" aria-label="Permalink to &quot;Skill Frontmatter Schema&quot;">​</a></h3><p>Each <code>SKILL.md</code> may contain YAML frontmatter with the following fields:</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>name</code></td><td><code>string</code></td><td>No</td><td>Display name (default: directory name)</td></tr><tr><td><code>description</code></td><td><code>string</code></td><td>No</td><td>Short description for autocomplete</td></tr><tr><td><code>allowed-tools</code></td><td><code>string[]</code></td><td>No</td><td>Tools the skill is allowed to use</td></tr><tr><td><code>context</code></td><td><code>string</code></td><td>No</td><td>Execution context: <code>fork</code>, <code>agent</code></td></tr><tr><td><code>model</code></td><td><code>string</code></td><td>No</td><td>Override model for this skill</td></tr><tr><td><code>max-turns</code></td><td><code>number</code></td><td>No</td><td>Maximum conversation turns</td></tr><tr><td><code>invocation</code></td><td><code>string</code></td><td>No</td><td>Invocation method: <code>user</code>, <code>auto-invoke</code>, <code>model-only</code></td></tr></tbody></table><p>If no frontmatter is found, the directory name is used as the command name.</p><h3 id="variable-substitution" tabindex="-1">Variable Substitution <a class="header-anchor" href="#variable-substitution" aria-label="Permalink to &quot;Variable Substitution&quot;">​</a></h3><p>Skill content supports variable substitution before injection:</p><table tabindex="0"><thead><tr><th>Variable</th><th>Description</th></tr></thead><tbody><tr><td><code>$ARGUMENTS</code></td><td>User-provided arguments after the command</td></tr><tr><td><code>\${CLAUDE_SESSION_ID}</code></td><td>Current session identifier</td></tr><tr><td><code>\${CLAUDE_MODEL}</code></td><td>Current model identifier</td></tr><tr><td><code>\${PROJECT_DIR}</code></td><td>Project root directory path</td></tr><tr><td><code>\${USER_HOME}</code></td><td>User home directory path</td></tr></tbody></table><p>Variables are substituted at invocation time, not at discovery time.</p><h3 id="shell-command-preprocessing" tabindex="-1">Shell Command Preprocessing <a class="header-anchor" href="#shell-command-preprocessing" aria-label="Permalink to &quot;Shell Command Preprocessing&quot;">​</a></h3><p>Skill content supports inline shell command execution using the <code>!\`command\`</code> syntax. The shell command is executed and its stdout replaces the markup in the skill content before injection. This enables dynamic content like file listings or environment values.</p><h3 id="skill-execution-features" tabindex="-1">Skill Execution Features <a class="header-anchor" href="#skill-execution-features" aria-label="Permalink to &quot;Skill Execution Features&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Feature</th><th>Value</th><th>Description</th></tr></thead><tbody><tr><td><code>context: fork</code></td><td>Fork context</td><td>Skill runs in a forked session, preserving the parent context</td></tr><tr><td><code>context: agent</code></td><td>Agent context</td><td>Skill runs as a sub-agent with its own isolated session</td></tr><tr><td><code>allowed-tools</code></td><td>Tool whitelist</td><td>Restricts which tools the skill can use during execution</td></tr></tbody></table><h3 id="skill-invocation-methods" tabindex="-1">Skill Invocation Methods <a class="header-anchor" href="#skill-invocation-methods" aria-label="Permalink to &quot;Skill Invocation Methods&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Method</th><th>Trigger</th><th>Description</th></tr></thead><tbody><tr><td><code>user</code></td><td>User types <code>/skillname</code></td><td>Default — user explicitly invokes via slash command</td></tr><tr><td><code>auto-invoke</code></td><td>Model decides</td><td>Model can invoke the skill automatically when relevant</td></tr><tr><td><code>model-only</code></td><td>Model-initiated only</td><td>Not shown in user autocomplete, model-only access</td></tr></tbody></table><h3 id="skill-execution" tabindex="-1">Skill Execution <a class="header-anchor" href="#skill-execution" aria-label="Permalink to &quot;Skill Execution&quot;">​</a></h3><p>When a skill slash command is selected, the full SKILL.md content (after variable substitution and shell preprocessing) is injected into the session prompt wrapped in <code>&lt;skill&gt;</code> tags. The model receives both the skill instructions and any user-provided arguments.</p><p><code>interactiveSession.submit(input, displayInput, rawInput)</code> is called with three arguments:</p><ul><li><code>input</code> — the expanded skill content for the model</li><li><code>displayInput</code> — the display form shown to the user (e.g., <code>/audit</code>)</li><li><code>rawInput</code> — the qualified name form used for hook matching (e.g., <code>/rulebased-harness:audit some-args</code>); if no qualified name is found, falls back to <code>displayInput</code></li></ul><p>The qualified name is resolved via <code>registry.resolveQualifiedName(cmd)</code> so that hook matchers can identify which plugin&#39;s skill was invoked.</p><h2 id="type-ownership" tabindex="-1">Type Ownership <a class="header-anchor" href="#type-ownership" aria-label="Permalink to &quot;Type Ownership&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Type</th><th>Location</th><th>Purpose</th></tr></thead><tbody><tr><td>ITerminalOutput</td><td><code>src/types.ts</code></td><td>Terminal I/O DI interface (duplicate — SSOT is agent-core)</td></tr><tr><td>ISpinner</td><td><code>src/types.ts</code></td><td>Spinner handle (duplicate — SSOT is agent-core)</td></tr><tr><td>IPermissionRequest</td><td><code>src/ui/types.ts</code></td><td>Permission prompt React state</td></tr><tr><td>ISlashCommand</td><td><code>src/commands/types.ts</code></td><td>CLI alias for <code>ICommand</code> from agent-sdk</td></tr><tr><td>ICommandSource</td><td><code>src/commands/types.ts</code></td><td>Re-export of <code>ICommandSource</code> from agent-sdk</td></tr></tbody></table><h2 id="public-api-surface" tabindex="-1">Public API Surface <a class="header-anchor" href="#public-api-surface" aria-label="Permalink to &quot;Public API Surface&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Export</th><th>Kind</th><th>Description</th></tr></thead><tbody><tr><td>startCli</td><td>function</td><td>CLI entry point</td></tr><tr><td>ITerminalOutput</td><td>type</td><td>Terminal I/O DI interface</td></tr><tr><td>ISpinner</td><td>type</td><td>Spinner handle</td></tr></tbody></table><p>Note: <code>createSession()</code> is internal to <code>agent-sdk</code> and is NOT re-exported. The CLI uses <code>InteractiveSession</code> directly. <code>index.ts</code> does not re-export SDK types; consumers should import those directly from <code>@robota-sdk/agent-sdk</code>.</p><h2 id="file-structure" tabindex="-1">File Structure <a class="header-anchor" href="#file-structure" aria-label="Permalink to &quot;File Structure&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/</span></span>
<span class="line"><span>├── bin.ts                           ← Binary entry point</span></span>
<span class="line"><span>├── cli.ts                           ← Config loading, Ink render invocation</span></span>
<span class="line"><span>├── print-terminal.ts                ← ITerminalOutput for print mode (-p)</span></span>
<span class="line"><span>├── types.ts                         ← ITerminalOutput, ISpinner</span></span>
<span class="line"><span>├── index.ts                         ← Re-exports (CommandRegistry, BuiltinCommandSource, etc.)</span></span>
<span class="line"><span>├── commands/</span></span>
<span class="line"><span>│   ├── types.ts                     ← ISlashCommand, ICommandSource interfaces</span></span>
<span class="line"><span>│   ├── builtin-source.ts            ← Re-export shim: \`export { BuiltinCommandSource } from &#39;@robota-sdk/agent-sdk&#39;\`</span></span>
<span class="line"><span>│   ├── command-registry.ts          ← Re-export shim: \`export { CommandRegistry } from &#39;@robota-sdk/agent-sdk&#39;\`</span></span>
<span class="line"><span>│   ├── skill-source.ts              ← Re-export shim: \`export { SkillCommandSource } from &#39;@robota-sdk/agent-sdk&#39;\`</span></span>
<span class="line"><span>│   ├── plugin-source.ts             ← PluginCommandSource (legacy local copy; main flow uses SDK version)</span></span>
<span class="line"><span>│   ├── skill-executor.ts            ← Skill execution helpers (fork/inject modes); not in main flow</span></span>
<span class="line"><span>│   │                                  (main flow uses buildSkillPrompt from @robota-sdk/agent-sdk)</span></span>
<span class="line"><span>│   └── slash-executor.ts            ← IPluginCallbacks interface + plugin TUI handler functions</span></span>
<span class="line"><span>│                                      (executeSlashCommand not in main flow; main flow uses session.executeCommand())</span></span>
<span class="line"><span>├── utils/</span></span>
<span class="line"><span>│   ├── cli-args.ts                  ← CLI argument parsing and validation</span></span>
<span class="line"><span>│   ├── settings-io.ts               ← Settings file read/write/update/delete</span></span>
<span class="line"><span>│   ├── provider-factory.ts          ← AI provider creation from settings</span></span>
<span class="line"><span>│   ├── tool-call-extractor.ts       ← Tool call display extraction from history</span></span>
<span class="line"><span>│   ├── paste-labels.ts              ← Paste label insertion and expansion for multiline paste</span></span>
<span class="line"><span>│   └── edit-diff.ts                 ← Edit diff computation and formatting for display</span></span>
<span class="line"><span>└── ui/</span></span>
<span class="line"><span>    ├── App.tsx                      ← Thin JSX shell (~220 lines); no queue/abort/session logic</span></span>
<span class="line"><span>    ├── hooks/</span></span>
<span class="line"><span>    │   ├── useInteractiveSession.ts ← ONLY React↔SDK bridge; delegates to TuiStateManager for</span></span>
<span class="line"><span>    │   │                              history: IHistoryEntry[] state; converts InteractiveSession</span></span>
<span class="line"><span>    │   │                              events to React state (streamingText, activeTools, etc.)</span></span>
<span class="line"><span>    │   ├── TuiStateManager.ts       ← Holds history: IHistoryEntry[]; syncs from getFullHistory();</span></span>
<span class="line"><span>    │   │                              manages windowing (MAX_RENDERED_MESSAGES) and local event entries</span></span>
<span class="line"><span>    │   └── usePluginCallbacks.ts    ← Plugin TUI callback wiring</span></span>
<span class="line"><span>    ├── render.tsx                   ← Ink render() invocation</span></span>
<span class="line"><span>    ├── MessageList.tsx              ← Renders IHistoryEntry[] via EntryItem (dispatches on category)</span></span>
<span class="line"><span>    ├── InputArea.tsx                ← Bottom fixed input (CjkTextInput), slash detection</span></span>
<span class="line"><span>    ├── StatusBar.tsx                ← Mode, model, context %, message count, Thinking</span></span>
<span class="line"><span>    ├── PermissionPrompt.tsx         ← Allow/Deny arrow-key selection (useInput)</span></span>
<span class="line"><span>    ├── StreamingIndicator.tsx       ← Real-time Tools:/Robota: display during run()</span></span>
<span class="line"><span>    ├── SlashAutocomplete.tsx        ← Command autocomplete popup (scroll, highlight)</span></span>
<span class="line"><span>    ├── CjkTextInput.tsx             ← Custom text input with Korean IME support</span></span>
<span class="line"><span>    ├── ConfirmPrompt.tsx            ← Reusable arrow-key confirmation prompt</span></span>
<span class="line"><span>    ├── WaveText.tsx                 ← Wave color animation for waiting indicator</span></span>
<span class="line"><span>    ├── DiffBlock.tsx                ← Diff block rendering for Edit tool output display</span></span>
<span class="line"><span>    ├── MenuSelect.tsx               ← Arrow-key menu selection component (Plugin TUI)</span></span>
<span class="line"><span>    ├── PluginTUI.tsx                ← Plugin management TUI (screen stack navigation)</span></span>
<span class="line"><span>    ├── TextPrompt.tsx               ← Text input prompt component (Plugin TUI)</span></span>
<span class="line"><span>    ├── plugin-tui-handlers.ts       ← Plugin TUI action handlers (install, uninstall, etc.)</span></span>
<span class="line"><span>    ├── render-markdown.ts           ← Markdown rendering for terminal output</span></span>
<span class="line"><span>    ├── InkTerminal.ts               ← No-op ITerminalOutput</span></span>
<span class="line"><span>    └── types.ts                     ← IPermissionRequest</span></span></code></pre></div><p><strong>Note:</strong> <code>CommandRegistry</code>, <code>BuiltinCommandSource</code>, <code>SkillCommandSource</code>, <code>PluginCommandSource</code>, and <code>SystemCommandExecutor</code> are all owned by <code>@robota-sdk/agent-sdk</code>. The CLI does not use <code>SystemCommandExecutor</code> directly; slash command execution goes through <code>session.executeCommand(name, args)</code>. The CLI&#39;s <code>src/commands/</code> directory holds re-export shims (<code>builtin-source.ts</code>, <code>command-registry.ts</code>, <code>skill-source.ts</code>) for backward compatibility, plus <code>slash-executor.ts</code> (plugin TUI handlers and IPluginCallbacks interface) and <code>skill-executor.ts</code> (fork/inject execution helpers). The CLI&#39;s <code>src/index.ts</code> exports only <code>startCli</code> and local CLI types.</p><h2 id="cli-usage" tabindex="-1">CLI Usage <a class="header-anchor" href="#cli-usage" aria-label="Permalink to &quot;CLI Usage&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">robota</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                              # Interactive TUI</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">robota</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -p</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;prompt&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                  # Print mode (one-shot)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">robota</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                           # Continue last session</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">robota</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --reset</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                      # Delete user settings and exit</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">robota</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -r</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                      # Resume session</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">robota</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --model</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">mode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">l</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">              # Model override</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">robota</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --language</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">lan</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">g</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            # Response language (ko, en, ja, zh)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">robota</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --permission-mode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">mod</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">e</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # plan | default | acceptEdits | bypassPermissions</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">robota</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --max-turns</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">n</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">              # Limit turns</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">robota</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --version</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                    # Version</span></span></code></pre></div><h2 id="tool-output-limits" tabindex="-1">Tool Output Limits <a class="header-anchor" href="#tool-output-limits" aria-label="Permalink to &quot;Tool Output Limits&quot;">​</a></h2><ul><li><strong>Universal cap</strong>: Tool output is capped at 30,000 characters. Outputs exceeding this limit are middle-truncated (first and last portions are kept, with a truncation marker in the middle).</li><li><strong>Glob entry limit</strong>: The Glob tool defaults to a maximum of 1,000 entries per invocation to prevent oversized responses.</li></ul><h2 id="first-run-setup" tabindex="-1">First-Run Setup <a class="header-anchor" href="#first-run-setup" aria-label="Permalink to &quot;First-Run Setup&quot;">​</a></h2><p>When no settings file exists (<code>~/.robota/settings.json</code>, <code>.robota/settings.json</code>, or <code>.robota/settings.local.json</code>), the CLI prompts for initial setup:</p><ol><li><strong>Anthropic API key</strong> (input masked with asterisks)</li><li><strong>Response language</strong> (ko/en/ja/zh, default: en)</li></ol><p>Creates <code>~/.robota/settings.json</code> with provider config and language setting. The language is injected into the system prompt as <code>&quot;Always respond in {language}.&quot;</code> and persists across compaction.</p><p>Use <code>robota --reset</code> to delete the user settings file and return to the first-run state.</p><h2 id="session-logging" tabindex="-1">Session Logging <a class="header-anchor" href="#session-logging" aria-label="Permalink to &quot;Session Logging&quot;">​</a></h2><p>Session logging is an SDK-internal concern. The CLI does not configure or manage log files. For logging details (JSONL format, log paths, event types), see the agent-sdk SPEC.</p><h2 id="tool-execution-display" tabindex="-1">Tool Execution Display <a class="header-anchor" href="#tool-execution-display" aria-label="Permalink to &quot;Tool Execution Display&quot;">​</a></h2><p>Tool execution uses a unified visual style across real-time streaming and post-execution summary.</p><h3 id="icons-and-colors" tabindex="-1">Icons and Colors <a class="header-anchor" href="#icons-and-colors" aria-label="Permalink to &quot;Icons and Colors&quot;">​</a></h3><table tabindex="0"><thead><tr><th>State</th><th>Icon</th><th>Color</th><th>Strikethrough</th><th>When</th></tr></thead><tbody><tr><td>Running</td><td>⟳</td><td>yellow</td><td>no</td><td>Tool is executing</td></tr><tr><td>Success</td><td>✓</td><td>green</td><td>no</td><td>Tool completed successfully</td></tr><tr><td>Error</td><td>✗</td><td>red</td><td>yes</td><td>Tool execution failed</td></tr><tr><td>Denied</td><td>⊘</td><td>yellowBright</td><td>yes</td><td>Permission denied</td></tr></tbody></table><h3 id="labels" tabindex="-1">Labels <a class="header-anchor" href="#labels" aria-label="Permalink to &quot;Labels&quot;">​</a></h3><ul><li><code>Tools:</code> / <code>Tool:</code> headers use <strong>white bold</strong> (visible on dark terminals).</li><li>Tool count badge: <code>[N tools]</code> in white dim.</li></ul><h3 id="argument-truncation" tabindex="-1">Argument Truncation <a class="header-anchor" href="#argument-truncation" aria-label="Permalink to &quot;Argument Truncation&quot;">​</a></h3><p>Long tool arguments are truncated with <strong>middle ellipsis</strong>, keeping the last 30 characters visible:</p><ul><li>Before: <code>Read(/Users/jungyoun/Documents/dev/robota/packages/agent-sdk/src/plugins/ver...)</code></li><li>After: <code>Read(/Users/jungyoun/Documents/dev/...sdk/src/plugins/very-long/file.ts)</code></li></ul><p>This ensures file names and important suffixes remain visible.</p><h3 id="plugin-skill-display" tabindex="-1">Plugin Skill Display <a class="header-anchor" href="#plugin-skill-display" aria-label="Permalink to &quot;Plugin Skill Display&quot;">​</a></h3><p>Plugin skills show the plugin hint before the description:</p><ul><li>Format: <code>/skill-name (plugin-name) description</code></li><li>Example: <code>/audit (rulebased-harness) Audits your project&#39;s harness setup</code></li></ul><h3 id="edit-diff-display" tabindex="-1">Edit Diff Display <a class="header-anchor" href="#edit-diff-display" aria-label="Permalink to &quot;Edit Diff Display&quot;">​</a></h3><p>When the Edit tool completes successfully, a compact diff is shown below the tool line. This gives the user immediate visibility into what changed without inspecting the file.</p><p><strong>Source:</strong> <code>old_string</code> and <code>new_string</code> from the Edit tool arguments.</p><p><strong>Display format:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>  ✓ Edit(src/provider.ts)</span></span>
<span class="line"><span>    │ src/provider.ts</span></span>
<span class="line"><span>    │ - const DEFAULT_MAX_TOKENS = 4096;</span></span>
<span class="line"><span>    │ + const maxTokens = getModelMaxOutput(modelId);</span></span></code></pre></div><p><strong>Rules:</strong></p><ul><li>Show the file path as a header line.</li><li>Removed lines in <strong>red</strong> with <code>-</code> prefix.</li><li>Added lines in <strong>green</strong> with <code>+</code> prefix.</li><li>Context lines (surrounding unchanged file content) shown in <strong>dim white</strong> — 2 lines before and after the changed region.</li><li><strong>Max display lines: 12.</strong> If the diff exceeds 12 lines, show the first 10 lines + <code>... and N more lines</code>.</li><li>If <code>old_string</code> and <code>new_string</code> are identical (no-op edit), show nothing.</li><li>Diff is shown in both the real-time streaming indicator (after tool completes) and the post-execution summary.</li></ul><p><strong>Permission prompt integration (future):</strong></p><p>When a permission prompt is shown for an Edit tool, the diff should be displayed alongside the Allow/Deny prompt so the user can see what will change before approving.</p><h2 id="keyboard-controls" tabindex="-1">Keyboard Controls <a class="header-anchor" href="#keyboard-controls" aria-label="Permalink to &quot;Keyboard Controls&quot;">​</a></h2><h3 id="message-display-order-fixed" tabindex="-1">Message Display Order (fixed) <a class="header-anchor" href="#message-display-order-fixed" aria-label="Permalink to &quot;Message Display Order (fixed)&quot;">​</a></h3><p>The display order is <strong>Tool → Robota</strong>, fixed and identical for streaming, normal completion, and ESC abort:</p><p><strong>During streaming (real-time):</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>You: [user prompt]             ← MessageList (visible immediately on submit)</span></span>
<span class="line"><span>System: Invoking skill: audit  ← MessageList (visible immediately, skills only)</span></span>
<span class="line"><span>Tool: ⟳ Read(file.ts)         ← StreamingIndicator (real-time, below MessageList)</span></span>
<span class="line"><span>      ⟳ Edit(file.ts)</span></span>
<span class="line"><span>Robota: [streaming text...]    ← StreamingIndicator (real-time)</span></span></code></pre></div><p><code>You:</code> and <code>System:</code> messages are visible from the start of streaming — not delayed until completion. Messages are synced from InteractiveSession on both <code>thinking=true</code> (execution start) and <code>thinking=false</code> (execution end). Only <code>Tool:</code> and <code>Robota:</code> are handled by StreamingIndicator during streaming.</p><p><strong>After completion or abort (final state):</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>You: [user prompt]             ← MessageList</span></span>
<span class="line"><span>Tool: ✓ Read(file.ts)         ← MessageList (tool summary message, inserted before Robota)</span></span>
<span class="line"><span>      ✓ Edit(file.ts)</span></span>
<span class="line"><span>Robota: [response]             ← MessageList</span></span>
<span class="line"><span>System: Interrupted by user.   ← MessageList (abort only)</span></span></code></pre></div><p><strong>Mechanism:</strong></p><ul><li>During streaming: <code>StreamingIndicator</code> renders <code>activeTools</code> + <code>streamingText</code> in real-time (Tool → Robota order).</li><li>On complete/interrupt/error: <code>InteractiveSession.pushToolSummaryMessage()</code> inserts a formatted tool summary into the <code>messages</code> array BEFORE the Robota response. Then <code>activeTools</code> is cleared and <code>StreamingIndicator</code> disappears.</li><li>Result: Tool → Robota order is preserved in both real-time and final state. Tool information transitions from <code>StreamingIndicator</code> (live) to <code>MessageList</code> (permanent).</li></ul><h3 id="ctrl-c-—-process-exit" tabindex="-1">Ctrl+C — Process Exit <a class="header-anchor" href="#ctrl-c-—-process-exit" aria-label="Permalink to &quot;Ctrl+C — Process Exit&quot;">​</a></h3><p>Ctrl+C always exits the process immediately. This is handled by Ink&#39;s <code>exitOnCtrlC: true</code> option at the render level, bypassing all <code>useInput</code> handlers. It works regardless of which UI overlay is active (PluginTUI, permission prompt, etc.).</p><h3 id="esc-—-abort-execution" tabindex="-1">ESC — Abort Execution <a class="header-anchor" href="#esc-—-abort-execution" aria-label="Permalink to &quot;ESC — Abort Execution&quot;">​</a></h3><p>ESC aborts the current execution gracefully (unlike Ctrl+C which kills the process):</p><ol><li>ESC key handler in <code>App.tsx</code> calls <code>handleAbort()</code> (from <code>useInteractiveSession</code>)</li><li><code>handleAbort</code> sets <code>isAborting: true</code> and calls <code>interactiveSession.abort()</code></li><li>AbortSignal propagates through the entire stack (ExecutionService -&gt; Provider -&gt; <code>streamWithAbort</code>)</li><li><code>executeRound</code> calls <code>commitAssistant(&#39;interrupted&#39;)</code> — the partial response is saved to conversation history with <code>state: &#39;interrupted&#39;</code>. Text is ALWAYS preserved (no stripping).</li><li><code>InteractiveSession</code> emits the <code>interrupted</code> event; the <code>thinking</code> event fires with <code>false</code></li></ol><p><strong>Rendering state on abort (<code>onInterrupted</code> handler):</strong></p><ul><li><strong>Tool list</strong>: <code>pushToolSummaryMessage()</code> inserts tool summary into <code>messages</code> (before Robota). Then <code>activeTools</code> is cleared — tool info lives in <code>MessageList</code> now, not <code>StreamingIndicator</code>.</li><li><strong>Streaming text</strong>: cleared (<code>streamBuf = &#39;&#39;</code>, <code>setStreamingText(&#39;&#39;)</code>). The interrupted response is committed to message history.</li><li><strong>isAborting</strong>: cleared by <code>onThinking(false)</code> handler.</li><li><strong>Border color</strong>: yellow (aborting) → green (normal) after <code>onThinking(false)</code>.</li></ul><ol start="6"><li><code>useInteractiveSession</code>&#39;s <code>onThinking(false)</code> handler: <ul><li>Sets <code>isAborting: false</code></li><li>Re-syncs <code>messages</code> from <code>interactiveSession.getMessages()</code> — interrupted messages are already committed</li><li>Messages with <code>msg.state === &#39;interrupted&#39;</code> show an interrupted indicator in the UI</li></ul></li><li>After abort, conversation continues normally — history includes the interrupted assistant message and any tool results</li><li>History is the SSOT for all message content. Append-only, read-only — no edit, no delete.</li></ol><p><strong>What appears in the UI after ESC:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Tool:                           ← in MessageList (from pushToolSummaryMessage)</span></span>
<span class="line"><span>  ✓ Read(file.ts)</span></span>
<span class="line"><span>  ⟳ Edit(file.ts)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Robota:                         ← in MessageList (committed interrupted response)</span></span>
<span class="line"><span>  [partial response text...]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>System:                         ← in MessageList</span></span>
<span class="line"><span>  Interrupted by user.</span></span></code></pre></div><p>Tool → Robota order preserved. StreamingIndicator is cleared (activeTools = []).</p><h3 id="up-down-arrows-—-visual-line-navigation" tabindex="-1">Up/Down Arrows — Visual Line Navigation <a class="header-anchor" href="#up-down-arrows-—-visual-line-navigation" aria-label="Permalink to &quot;Up/Down Arrows — Visual Line Navigation&quot;">​</a></h3><p>When input text wraps across multiple visual lines (exceeds terminal width), up/down arrows move the cursor between visual lines using display offset arithmetic.</p><p><strong>Architecture:</strong></p><ul><li>Cursor-only manipulation — text is never modified, only <code>cursorRef</code> position changes</li><li>Two private helpers in <code>CjkTextInput.tsx</code> (no separate module): <ul><li><code>displayOffset(chars, charIndex, width)</code> → cumulative display column offset, accounting for CJK line-end gaps</li><li><code>charIndexAtDisplayOffset(chars, targetOffset, width)</code> → char index closest to target offset</li></ul></li><li>Up arrow: <code>cursorRef = charIndexAtDisplayOffset(chars, offset - availableWidth, width)</code></li><li>Down arrow: <code>cursorRef = charIndexAtDisplayOffset(chars, offset + availableWidth, width)</code></li><li>Uses <code>string-width</code> for CJK character support (2 columns per CJK character)</li></ul><p><strong>Available width calculation:</strong></p><ul><li><code>InputArea</code> computes <code>availableWidth</code> from <code>useStdout().columns</code> minus layout constants</li><li><code>availableWidth = terminalColumns - BORDER_HORIZONTAL - PADDING_LEFT - PROMPT_WIDTH</code></li><li>Named constants (no magic numbers): <code>BORDER_HORIZONTAL = 2</code>, <code>PADDING_LEFT = 1</code>, <code>PROMPT_WIDTH = 2</code> (&quot;&gt; &quot;)</li><li>Layout constants are co-located with InputArea (the component that owns the layout)</li><li><code>availableWidth</code> is passed to <code>CjkTextInput</code> as a prop</li></ul><p><strong>Behavior:</strong></p><ul><li>Up arrow when already on first visual line: no-op (target offset &lt; 0)</li><li>Down arrow when already on last visual line: no-op (target offset exceeds text)</li><li>Column position is preserved across line moves via offset arithmetic</li><li>Terminal resize recalculates available width via <code>useStdout()</code></li></ul><h3 id="paste-handling" tabindex="-1">Paste Handling <a class="header-anchor" href="#paste-handling" aria-label="Permalink to &quot;Paste Handling&quot;">​</a></h3><p><strong>Bracketed paste mode (DECSET 2004):</strong></p><ul><li><code>render.tsx</code> enables on startup (<code>\\x1b[?2004h</code>), disables on exit (<code>\\x1b[?2004l</code>)</li><li>Only enabled when <code>process.stdin.isTTY &amp;&amp; process.stdout.isTTY</code></li><li>Terminal wraps pasted content with <code>\\x1b[200~</code> (start) and <code>\\x1b[201~</code> (end) markers</li><li>Ink&#39;s CSI parser strips the ESC prefix, so <code>useInput</code> receives <code>[200~</code> and <code>[201~</code></li><li><code>CjkTextInput</code> detects these markers and buffers all input between them</li><li>On paste-end marker, the complete buffer is flushed with <code>\\r\\n</code>/<code>\\r</code> normalized to <code>\\n</code></li><li>Deterministic boundary detection — no debounce or timing heuristics</li></ul><p><strong>Single-line vs multiline paste:</strong></p><ul><li>Single-line paste (no <code>\\n</code>): inserted directly into the input as typed text</li><li>Multiline paste (contains <code>\\n</code>): routed to <code>onPaste</code> → <code>InputArea.handlePaste</code> creates a <code>[Pasted text #N +M lines]</code> label in the input field, stores content in <code>pasteStore</code></li><li>On submit, <code>expandPasteLabels()</code> replaces labels with actual content from <code>pasteStore</code></li><li>Paste store is cleared after each submit</li></ul><p><strong>Fallback for terminals without bracketed paste:</strong></p><ul><li>Multi-char input containing <code>\\n</code> or <code>\\r</code> is treated as a single paste (original heuristic)</li></ul><h2 id="plugin-management-tui" tabindex="-1">Plugin Management TUI <a class="header-anchor" href="#plugin-management-tui" aria-label="Permalink to &quot;Plugin Management TUI&quot;">​</a></h2><p>The <code>/plugin</code> command opens an interactive TUI for managing bundle plugins, built with <code>MenuSelect</code>, <code>TextPrompt</code>, and <code>ConfirmPrompt</code> components.</p><h3 id="screen-stack-navigation" tabindex="-1">Screen Stack Navigation <a class="header-anchor" href="#screen-stack-navigation" aria-label="Permalink to &quot;Screen Stack Navigation&quot;">​</a></h3><p>The TUI uses a screen stack pattern with 8 screens:</p><table tabindex="0"><thead><tr><th>Screen</th><th>Description</th></tr></thead><tbody><tr><td><code>main</code></td><td>Top-level menu (Marketplace / Installed / Exit)</td></tr><tr><td><code>marketplace-list</code></td><td>List of configured marketplace sources</td></tr><tr><td><code>marketplace-action</code></td><td>Actions for a selected source (Browse / Add / Back)</td></tr><tr><td><code>marketplace-browse</code></td><td>Browse plugins from a selected source</td></tr><tr><td><code>marketplace-install-scope</code></td><td>Choose install scope (project / user)</td></tr><tr><td><code>marketplace-add</code></td><td>Add a new marketplace source URL</td></tr><tr><td><code>installed-list</code></td><td>List of installed plugins with enable/disable state</td></tr><tr><td><code>installed-action</code></td><td>Actions for a selected plugin (Enable/Disable / Uninstall / Back)</td></tr></tbody></table><p>ESC navigates back in the stack. When the stack is empty, the TUI closes and returns to the normal input area.</p><h2 id="subagent-execution" tabindex="-1">Subagent Execution <a class="header-anchor" href="#subagent-execution" aria-label="Permalink to &quot;Subagent Execution&quot;">​</a></h2><p>Subagent execution (Agent tool, fork sessions, agent definition loading) is managed entirely by <code>@robota-sdk/agent-sdk</code> internally. The CLI does not wire these components directly — <code>InteractiveSession</code> handles all subagent lifecycle.</p><p>For implementation details of subagent execution (Agent tool, <code>context: fork</code> skills, agent definition scanning), see the agent-sdk SPEC.</p><h2 id="memory-management" tabindex="-1">Memory Management <a class="header-anchor" href="#memory-management" aria-label="Permalink to &quot;Memory Management&quot;">​</a></h2><h3 id="message-windowing" tabindex="-1">Message Windowing <a class="header-anchor" href="#message-windowing" aria-label="Permalink to &quot;Message Windowing&quot;">​</a></h3><p><code>TuiStateManager</code> keeps only the most recent 100 entries (<code>MAX_RENDERED_MESSAGES</code>) in <code>history: IHistoryEntry[]</code>. Older entries are dropped from the render tree to prevent unbounded memory growth. Full conversation history is preserved in the session store on disk.</p><h3 id="tool-state-cleanup" tabindex="-1">Tool State Cleanup <a class="header-anchor" href="#tool-state-cleanup" aria-label="Permalink to &quot;Tool State Cleanup&quot;">​</a></h3><p>Completed tool execution states are trimmed to the most recent 50 entries (<code>MAX_COMPLETED_TOOLS</code>). Running tools are always kept. This prevents <code>activeTools</code> array from growing unbounded during tool-heavy responses.</p><h3 id="react-memo" tabindex="-1">React.memo <a class="header-anchor" href="#react-memo" aria-label="Permalink to &quot;React.memo&quot;">​</a></h3><p><code>MessageItem</code> component uses <code>React.memo</code> to skip re-renders when message props are unchanged, reducing CPU and indirect memory pressure from Ink&#39;s full-tree reconciliation.</p><h2 id="message-architecture" tabindex="-1">Message Architecture <a class="header-anchor" href="#message-architecture" aria-label="Permalink to &quot;Message Architecture&quot;">​</a></h2><p>The CLI uses <code>IHistoryEntry</code> (from <code>@robota-sdk/agent-core</code>, re-exported by <code>@robota-sdk/agent-sdk</code>) as the primary message type for the message list. <code>TUniversalMessage</code> is still used in lower-level contexts (session history access, type guards, provider calls). There is no local <code>IChatMessage</code> type.</p><h3 id="type-unification" tabindex="-1">Type Unification <a class="header-anchor" href="#type-unification" aria-label="Permalink to &quot;Type Unification&quot;">​</a></h3><ul><li><code>IHistoryEntry[]</code> is the primary type held by <code>TuiStateManager</code> and passed to <code>MessageList</code></li><li><code>MessageList</code> renders entries via <code>EntryItem</code>, which dispatches on <code>entry.category</code>: <ul><li><code>&#39;chat&#39;</code> entries: rendered as conversation messages (user, assistant, system, tool)</li><li><code>&#39;event&#39;</code> entries: rendered based on <code>entry.type</code> (e.g., <code>&#39;tool-summary&#39;</code> renders the tool call list, <code>&#39;skill-invocation&#39;</code> renders a system notice)</li></ul></li><li><code>entry.id</code> (UUID) is used as the React key for message list rendering</li><li><code>TUniversalMessage</code> is still used where needed (type guards, provider API calls, <code>getMessages()</code> for backward compat)</li><li><code>msg.state === &#39;interrupted&#39;</code> shows an interrupted indicator in the UI</li></ul><h3 id="message-state-in-useinteractivesession" tabindex="-1">Message State in useInteractiveSession <a class="header-anchor" href="#message-state-in-useinteractivesession" aria-label="Permalink to &quot;Message State in useInteractiveSession&quot;">​</a></h3><ul><li><code>history: IHistoryEntry[]</code> React state is managed by <code>TuiStateManager</code> and derived from <code>interactiveSession.getFullHistory()</code>.</li><li>After each execution (when <code>thinking</code> transitions to <code>false</code>), the hook delegates to <code>TuiStateManager</code> to sync <code>history</code> from <code>interactiveSession.getFullHistory()</code> — the session is the SSOT for all history content.</li><li><code>addMessage</code> appends a local system message directly to React state (used for command output and error notices that are not part of the AI conversation). These are wrapped as <code>IHistoryEntry</code> with <code>category: &#39;event&#39;</code> before insertion.</li><li>After abort: interrupted messages are already committed to session history by <code>InteractiveSession</code>; the hook re-syncs from full history — no separate streaming text ref is needed.</li></ul><h3 id="tool-message-type-guards" tabindex="-1">Tool Message Type Guards <a class="header-anchor" href="#tool-message-type-guards" aria-label="Permalink to &quot;Tool Message Type Guards&quot;">​</a></h3><p>Tool messages use the <code>isToolMessage(msg)</code> type guard for safe access to <code>msg.name</code>.</p><h2 id="known-limitations" tabindex="-1">Known Limitations <a class="header-anchor" href="#known-limitations" aria-label="Permalink to &quot;Known Limitations&quot;">​</a></h2><ul><li><strong>Korean IME on macOS Terminal.app</strong>: Ink&#39;s renderer shifts the input area during IME composition, causing Terminal.app to crash (SIGSEGV). Fixed by adding a permanent blank line below the input area, which stabilizes the cursor position during IME composition. <strong>Use <a href="https://iterm2.com/" target="_blank" rel="noreferrer">iTerm2</a> for the best experience.</strong></li><li><strong>CjkTextInput</strong>: Custom text input component with try-catch error handling, non-printable character filtering, <code>setCursorPosition</code> removed to minimize IME interaction surface, and visual-line-aware up/down arrow navigation for wrapped text.</li></ul><h2 id="dependencies" tabindex="-1">Dependencies <a class="header-anchor" href="#dependencies" aria-label="Permalink to &quot;Dependencies&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Package</th><th>Purpose</th></tr></thead><tbody><tr><td><code>@robota-sdk/agent-sdk</code></td><td><code>InteractiveSession</code>, <code>CommandRegistry</code>, command sources, plugin management</td></tr><tr><td><code>@robota-sdk/agent-core</code></td><td>Public types (<code>TPermissionMode</code>, <code>TToolArgs</code>, <code>TUniversalMessage</code>, etc.)</td></tr><tr><td><code>@robota-sdk/agent-provider-anthropic</code></td><td>Anthropic provider creation (CLI picks provider based on config)</td></tr><tr><td><code>ink</code>, <code>react</code></td><td>TUI rendering</td></tr><tr><td><code>ink-select-input</code></td><td>Arrow-key selection (permission prompt)</td></tr><tr><td><code>ink-spinner</code></td><td>Loading spinner</td></tr><tr><td><code>chalk</code></td><td>Terminal colors</td></tr><tr><td><code>ink-text-input</code></td><td>Base text input (extended by CjkTextInput)</td></tr><tr><td><code>marked</code>, <code>marked-terminal</code></td><td>Markdown parsing and terminal rendering</td></tr><tr><td><code>cli-highlight</code></td><td>Syntax highlighting for code blocks</td></tr><tr><td><code>string-width</code></td><td>Unicode-aware string width calculation</td></tr></tbody></table>`,207)]))}const m=t(o,[["render",i]]);export{u as __pageData,m as default};
